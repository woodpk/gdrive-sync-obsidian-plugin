import assert from "node:assert/strict";
import test from "node:test";
import type { BinaryContentSource, ChangeCursor, ContentEvidence, ManagedRemoteIdentity, ObservationToken, PathSnapshot, RemoteObjectId, StateLoadContext, TrustedSynchronizationState, VaultPath } from "../src/contracts";
import { contractId } from "../src/contracts";
import { ThreeWayConflictResolver } from "../src/core/conflict-resolver";
import { InMemoryRunLeasePort } from "../src/core/run-coordinator";
import { DeterministicSynchronizationPlanner } from "../src/core/planner";
import { ProductionSynchronizationPlanner } from "../src/core/production-planner";
import { BoundedAuditHistory, MemoryAuditPersistence } from "../src/product/audit-history";
import { IntegratedProductController } from "../src/product/product-controller";
import { ProductSynchronizationExecutor } from "../src/product/production-executor";
import { ProductSnapshotAssembler } from "../src/product/snapshot-assembler";
import { createInitialTrustedState, MemoryStateByteStorage, PersistentSynchronizationStateStore } from "../src/state/persistent-state-store";
import { sha256Text } from "../src/util/sha256";

const id=<T extends string>(v:string)=>contractId<T>(v),vp=(v:string)=>id<"VaultPath">(v) as VaultPath,rid=(v:string)=>id<"RemoteObjectId">(v) as RemoteObjectId,cur=(v:string)=>id<"ChangeCursor">(v) as ChangeCursor;
const ev=(t:string,revision?:string):ContentEvidence=>({hash:sha256Text(t),sizeBytes:new TextEncoder().encode(t).byteLength,...(revision?{revision}:{})});
const src=(t:string):BinaryContentSource=>({sizeBytes:new TextEncoder().encode(t).byteLength,async *openChunks(){yield new TextEncoder().encode(t);}});
async function text(s:BinaryContentSource){let out="";const d=new TextDecoder();for await(const c of s.openChunks())out+=d.decode(c,{stream:true});return out+d.decode();}

const vault=id<"VaultIdentity">("vault:g2:coord"),device=id<"DeviceIdentity">("device:g2:coord"),identity:ManagedRemoteIdentity={rootId:rid("root:g2:coord"),vaultIdentity:vault,protocolVersion:id<"ProtocolVersion">("1")};
const context:StateLoadContext={expectation:"existing-pairing",expectedVaultIdentity:vault,expectedDeviceIdentity:device};
function trusted(path:VaultPath,r:RemoteObjectId,t:string,stale=false):TrustedSynchronizationState{const s=createInitialTrustedState({stateRevision:id<"StateRevision">("state:0"),vaultIdentity:vault,deviceIdentity:device});return{...s,base:[{path,entityKind:"file",localExisted:true,remoteExisted:true,content:ev(t),remoteObjectId:r}],remoteMappings:[{path,entityKind:"file",remoteObjectId:r}],changeCursor:cur("cursor:old"),knownDevices:[{deviceId:device,stale}]};}
function resolver(){return new ThreeWayConflictResolver({readText:async()=>undefined});}
function planner(trigger:any){const c=resolver();return new ProductionSynchronizationPlanner(new DeterministicSynchronizationPlanner(c,undefined,{trigger}));}

async function controllerFrom(real:{local:any;drive:any;store:PersistentSynchronizationStateStore;assembler:any;lease?:any;holder?:string}){let controller!:IntegratedProductController;const conflicts=resolver();const executor=new ProductSynchronizationExecutor(real.local,real.drive,real.store,context,()=>controller.currentRunEvidence());controller=new IntegratedProductController({vaultIdentity:vault,deviceIdentity:device,stateContext:context,stateStore:real.store,snapshotAssembler:real.assembler,executor,conflictResolver:conflicts,plannerForTrigger:t=>new ProductionSynchronizationPlanner(new DeterministicSynchronizationPlanner(conflicts,undefined,{trigger:t})),leasePort:real.lease??new InMemoryRunLeasePort(),audit:new BoundedAuditHistory(new MemoryAuditPersistence(),100),holderId:real.holder??"g2-coord"});return controller;}

test("G2 scenario 20 invalid Changes cursor falls back to safe full Phase5 reconciliation and commits a fresh cursor",async()=>{const p=vp("cursor.md"),r=rid("rid:cursor"),store=new PersistentSynchronizationStateStore(new MemoryStateByteStorage());await store.saveTrusted(trusted(p,r,"same"));let changes=0,full=0;const local={enumerate:async()=>({entries:[{status:"present" as const,side:"local" as const,path:p,entityKind:"file" as const,content:ev("same"),stability:"stable" as const,observationToken:id<"ObservationToken">("tok") as ObservationToken}],completeness:{status:"complete" as const}}),observe:async()=>({status:"present" as const,side:"local" as const,path:p,entityKind:"file" as const,content:ev("same"),stability:"stable" as const,observationToken:id<"ObservationToken">("tok")}),readFile:async()=>({content:src("same"),evidence:ev("same"),stability:"stable" as const}),validatePath:async()=>({status:"compatible" as const,normalizedComparisonPath:String(p)})} as never;const drive={validateManagedRoot:async()=>({ok:true as const,value:{status:"valid" as const,identity}}),readChanges:async()=>{changes++;return{ok:false as const,signal:{kind:"not-found" as const}};},getStartCursor:async()=>({ok:true as const,value:cur("cursor:fresh")}),listForReconciliation:async()=>{full++;return{ok:true as const,value:{entries:[{path:p,entityKind:"file" as const,remoteObjectId:r,content:ev("same"),trashed:false}],completeness:{status:"complete" as const}}};},observe:async()=>({ok:true as const,value:{status:"present" as const,side:"remote" as const,path:p,entityKind:"file" as const,remoteObjectId:r,content:ev("same"),stability:"stable" as const}})} as never;const assembler=new ProductSnapshotAssembler(local,drive,store,context,async()=>identity);const c=await controllerFrom({local,drive,store,assembler});await c.runAutomatic("periodic");assert.equal(changes,1);assert.equal(full,1);const loaded=await store.load(context);assert.equal(loaded.status,"trusted");if(loaded.status==="trusted")assert.equal(loaded.state.changeCursor,cur("cursor:fresh"));});

test("G2 scenarios 21 and 22 incomplete remote or local observation cannot become deletion authority in Phase5 planning",async()=>{const p=vp("uncertain.md"),r=rid("rid:uncertain");for(const mode of ["remote-partial","local-partial"] as const){const store=new PersistentSynchronizationStateStore(new MemoryStateByteStorage());await store.saveTrusted(trusted(p,r,"base"));let trashed=0;const local={enumerate:async()=>mode==="local-partial"?{entries:[],completeness:{status:"partial" as const,reason:"unreadable subtree"}}:{entries:[{status:"present" as const,side:"local" as const,path:p,entityKind:"file" as const,content:ev("base"),stability:"stable" as const}],completeness:{status:"complete" as const}},trash:async()=>{trashed++;}} as never;const drive={validateManagedRoot:async()=>({ok:true as const,value:{status:"valid" as const,identity}}),getStartCursor:async()=>({ok:true as const,value:cur("cursor:new")}),listForReconciliation:async()=>({ok:true as const,value:{entries:[],completeness:mode==="remote-partial"?{status:"partial" as const,reason:"page incomplete"}:{status:"complete" as const}}}),trash:async()=>{trashed++;return{ok:true as const,value:undefined};}} as never;const assembler=new ProductSnapshotAssembler(local,drive,store,context,async()=>identity);const c=await controllerFrom({local,drive,store,assembler});const plan=await c.previewManual();assert.ok(plan);assert.equal(plan.operations.some(o=>o.kind==="blocked-unsafe"),true);assert.equal(trashed,0);}});

test("G2 scenario 23 stale current device cannot authorize destructive propagation through production controller planning",async()=>{const p=vp("stale-delete.md"),r=rid("rid:stale"),store=new PersistentSynchronizationStateStore(new MemoryStateByteStorage());await store.saveTrusted(trusted(p,r,"base",true));let trash=0;const local={enumerate:async()=>({entries:[],completeness:{status:"complete" as const}})} as never,drive={validateManagedRoot:async()=>({ok:true as const,value:{status:"valid" as const,identity}}),getStartCursor:async()=>({ok:true as const,value:cur("new")}),listForReconciliation:async()=>({ok:true as const,value:{entries:[{path:p,entityKind:"file" as const,remoteObjectId:r,content:ev("base"),trashed:false}],completeness:{status:"complete" as const}}}),trash:async()=>{trash++;return{ok:true as const,value:undefined};}} as never;const c=await controllerFrom({local,drive,store,assembler:new ProductSnapshotAssembler(local,drive,store,context,async()=>identity)});const plan=await c.previewManual();assert.ok(plan);assert.equal(plan.executionDisposition,"blocked");assert.equal(plan.operations.some(o=>o.kind==="blocked-unsafe"),true);assert.equal(trash,0);});

test("G2 scenario 40 missing expected managed root blocks Phase5 before planning or mutation",async()=>{const store=new PersistentSynchronizationStateStore(new MemoryStateByteStorage());let mutations=0;const local={enumerate:async()=>({entries:[],completeness:{status:"complete" as const}})} as never,drive={validateManagedRoot:async()=>({ok:true as const,value:{status:"missing-root" as const}}),create:async()=>{mutations++;throw new Error("must not mutate");}} as never;const assembler=new ProductSnapshotAssembler(local,drive,store,{expectation:"new-installation"},async()=>identity);let c!:IntegratedProductController;const executor=new ProductSynchronizationExecutor(local,drive,store,{expectation:"new-installation"},()=>c.currentRunEvidence());c=new IntegratedProductController({vaultIdentity:vault,deviceIdentity:device,stateContext:{expectation:"new-installation"},stateStore:store,snapshotAssembler:assembler,executor,conflictResolver:resolver(),plannerForTrigger:t=>planner(t),leasePort:new InMemoryRunLeasePort(),audit:new BoundedAuditHistory(new MemoryAuditPersistence(),20),holderId:"missing-root"});assert.equal(await c.previewManual(),undefined);assert.equal(c.currentSurface().status.kind,"recovery-required");assert.equal(mutations,0);});

function localOnlySnapshot(path:VaultPath,t:string):PathSnapshot{const e=ev(t),tok=id<"ObservationToken">(`tok:${String(path)}`) as ObservationToken;return{path,local:{status:"present",side:"local",path,entityKind:"file",content:e,stability:"stable",observationToken:tok},remote:{status:"absent",side:"remote",path},base:{status:"uninitialized"},remoteEnumeration:{status:"complete"},identity:{status:"unambiguous"}};}
async function coordinationHarness(paths:readonly string[],lease:any=new InMemoryRunLeasePort(),holder="coord"){const store=new PersistentSynchronizationStateStore(new MemoryStateByteStorage()),ctx={expectation:"new-installation" as const,expectedVaultIdentity:vault,expectedDeviceIdentity:device};const files=new Map(paths.map(p=>[p,p]));let createCalls:string[]=[],active=0,maxActive=0;let hold:string|undefined,release:(()=>void)|undefined;const started=new Map<string,Promise<void>>(),startedResolve=new Map<string,()=>void>();for(const p of paths)started.set(p,new Promise<void>(r=>startedResolve.set(p,r)));const local={observe:async(p:VaultPath)=>files.has(String(p))?{status:"present" as const,side:"local" as const,path:p,entityKind:"file" as const,content:ev(files.get(String(p))!),stability:"stable" as const,observationToken:id<"ObservationToken">(`tok:${String(p)}`)}:{status:"absent" as const,side:"local" as const,path:p},readFile:async(p:VaultPath)=>({content:src(files.get(String(p))!),evidence:ev(files.get(String(p))!),stability:"stable" as const,observationToken:id<"ObservationToken">(`tok:${String(p)}`)}),validatePath:async(p:VaultPath)=>({status:"compatible" as const,normalizedComparisonPath:String(p)})} as never;const remote=new Map<string,{r:RemoteObjectId;e:ContentEvidence}>();const drive={observe:async(_r:RemoteObjectId,p:VaultPath)=>{const x=remote.get(String(p));return x?{ok:true as const,value:{status:"present" as const,side:"remote" as const,path:p,entityKind:"file" as const,remoteObjectId:x.r,content:x.e,stability:"stable" as const}}:{ok:true as const,value:{status:"absent" as const,side:"remote" as const,path:p}};},create:async(_r:RemoteObjectId,q:{path:VaultPath;content?:BinaryContentSource;expectedEvidence?:ContentEvidence})=>{active++;maxActive=Math.max(maxActive,active);createCalls.push(String(q.path));startedResolve.get(String(q.path))?.();if(hold===String(q.path))await new Promise<void>(r=>release=r);const r=rid(`r:${String(q.path)}`),e=q.expectedEvidence??ev(await text(q.content!));remote.set(String(q.path),{r,e});active--;return{ok:true as const,value:{remoteObjectId:r,path:q.path,evidence:e}};}} as never;const assembly={input:{snapshots:paths.map(p=>localOnlySnapshot(vp(p),p)),state:{status:"uninitialized" as const}},managedRemote:identity,remoteEnumeration:{status:"complete" as const},nextCursor:cur("cursor:coord"),mode:"full" as const};let controller!:IntegratedProductController;const executor=new ProductSynchronizationExecutor(local,drive,store,ctx,()=>controller.currentRunEvidence());const conflicts=resolver();controller=new IntegratedProductController({vaultIdentity:vault,deviceIdentity:device,stateContext:ctx,stateStore:store,snapshotAssembler:{assemble:async()=>assembly,assembleFull:async()=>assembly} as never,executor,conflictResolver:conflicts,plannerForTrigger:t=>new ProductionSynchronizationPlanner(new DeterministicSynchronizationPlanner(conflicts,undefined,{trigger:t})),leasePort:lease,audit:new BoundedAuditHistory(new MemoryAuditPersistence(),50),holderId:holder});return{controller,store,createCalls,started,hold:(p:string)=>hold=p,release:()=>release?.(),maxActive:()=>maxActive};}

test("G2 scenario 27 cancellation stops future operations and leaves cursor unadvanced",async()=>{const h=await coordinationHarness(["a.md","b.md"]);h.hold("a.md");const run=h.controller.runAutomatic("periodic");await h.started.get("a.md");await h.controller.request({kind:"cancel-active-sync"});h.release();await run;assert.deepEqual(h.createCalls,["a.md"]);const loaded=await h.store.load({expectation:"new-installation",expectedVaultIdentity:vault,expectedDeviceIdentity:device});assert.equal(loaded.status,"trusted");if(loaded.status==="trusted")assert.equal(loaded.state.changeCursor,undefined);});

test("G2 scenario 28 pause blocks product-controller synchronization until resume",async()=>{const h=await coordinationHarness(["pause.md"]);assert.equal((await h.controller.request({kind:"pause"})).status,"accepted");await h.controller.runAutomatic("periodic");assert.deepEqual(h.createCalls,[]);assert.equal(h.controller.currentSurface().status.kind,"paused");assert.equal((await h.controller.request({kind:"resume"})).status,"accepted");await h.controller.runAutomatic("periodic");assert.deepEqual(h.createCalls,["pause.md"]);});

test("G2 scenario 29 same-runtime product synchronization runs serialize rather than overlap",async()=>{const h=await coordinationHarness(["serial.md"]);h.hold("serial.md");const first=h.controller.runAutomatic("periodic");await h.started.get("serial.md");const second=h.controller.runAutomatic("periodic");await Promise.resolve();assert.equal(h.maxActive(),1);h.release();await Promise.all([first,second]);assert.equal(h.maxActive(),1);});

test("G2 scenario 30 two real controller runs sharing production lease cannot mutate concurrently",async()=>{const lease=new InMemoryRunLeasePort(),a=await coordinationHarness(["lease-a.md"],lease,"a"),b=await coordinationHarness(["lease-b.md"],lease,"b");a.hold("lease-a.md");const first=a.controller.runAutomatic("periodic");await a.started.get("lease-a.md");await b.controller.runAutomatic("periodic");assert.deepEqual(b.createCalls,[]);a.release();await first;});
