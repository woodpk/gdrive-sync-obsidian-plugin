# VH36 — E06 Authentication / Network / Quota Failure Semantics

Agent: `agt-ca-p6-vh36-e06-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh36-e06-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh36-e06-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh36-e06.ps1`
## Assignment
Implement only the E06 hybrid harness scenario and focused tests with separate subcases for offline-network failure, invalid/revoked-auth semantics, and safely inducible rate/quota-style failure. Use the deterministic fault layer for repeatable production-boundary semantics, verify local editing remains usable where required, remote mutation stops/defers safely, retry is bounded, no destructive fallback occurs, and normal production behavior resumes after the fault is cleared. Include a resumable human checkpoint only for any genuine authentication restoration/system-browser action that the live scenario still requires.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt value is filled later.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `E06-auth-network-quota.md`, VH15 evidence, current auth/network/retry/Drive failure code, H4 fault APIs, and checkpoint APIs. Do not intentionally exhaust real account storage or rate quota. Do not treat deterministic invalid-auth/rate-limit injection as proof that a real external account revocation occurred. Preserve ordinary OAuth scope and production failure semantics. Do not modify frozen H0 contracts or `src/contracts/**`.

Required end state: all three subcases are independently selectable/evidenced; unsupported real quota exhaustion is never attempted; deterministic tests prove fail-closed remote mutation, bounded retry/defer, local usability, and safe recovery, plus checkpoint-resume behavior where applicable.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh36-e06.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, subcases, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E06 PASS claim.