# VH34 — E04 Partial Listing and Missing Remote Root

Agent: `agt-ca-p6-vh34-e04-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh34-e04-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh34-e04-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh34-e04.ps1`
## Assignment
Implement only the E04 harness scenario and focused tests using a dedicated disposable validation remote. Subcase 1: inject an incomplete required full remote enumeration and verify absence-based deletion is never authorized. Subcase 2: after restoring normal enumeration, make only the disposable paired remote root unavailable/trashed through an approved validation seam and verify critical recovery behavior, no silent replacement root, and no bulk re-upload. Preserve enough evidence to restore/identify the disposable remote after the scenario.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field is deferred.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol/OBS-01, `E04-remote-coverage-failures.md`, VH15 evidence, current Drive enumeration/managed-root/recovery code, sandbox, and H4 fault APIs. Never manipulate the user's ordinary paired remote to manufacture this test. Incomplete coverage must remain explicitly incomplete and cannot authorize absence. Do not modify frozen H0 contracts or `src/contracts/**`; do not run the physical disposable-remote scenario during this build.

Required end state: both subcases are isolated and deterministic in test doubles, enforce disposable-remote ownership, and prove no absence deletion/silent-root replacement/bulk re-upload.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh34-e04.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E04 PASS claim.