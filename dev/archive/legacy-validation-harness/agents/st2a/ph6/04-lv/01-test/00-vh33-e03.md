# VH33 — E03 Corrupt State and Lost Cursor Recovery

Agent: `agt-ca-p6-vh33-e03-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh33-e03-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh33-e03-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh33-e03.ps1`
## Assignment
Implement only the E03 harness scenario and focused tests with two bounded subcases against a dedicated disposable validation vault/state copy. A: create a backup/checkpoint, corrupt or truncate the disposable authoritative local sync state through the approved validation-only seam, and verify explicit untrusted/recovery behavior with no deletion inference. B: restore clean disposable state, invalidate/remove only the remote change cursor through the approved seam, and verify conservative full reconciliation. Both subcases must preserve content and re-establish trustworthy state through production recovery paths.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt data requires later filling.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol/OBS-01, `E03-state-and-cursor-recovery.md`, VH15 evidence, current persistence/cursor/recovery code, sandbox and H4 fault/state-manipulation APIs. Never mutate primary BRAIN state, and never make missing/corrupt state count as deletion authority. Do not change frozen H0 contracts or `src/contracts/**`. Do not run against the user's live primary state.

Required end state: backup provenance and exact disposable manipulation are evidenced; A and B remain separate; deterministic tests prove fail-closed recovery, full-reconcile fallback, content preservation, and refusal outside disposable scope.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh33-e03.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E03 PASS claim.