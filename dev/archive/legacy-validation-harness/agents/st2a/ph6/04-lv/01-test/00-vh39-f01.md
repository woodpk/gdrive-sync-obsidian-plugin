# VH39 — F01 Filesystem Scope / Paths / Opaque Files

Agent: `agt-ca-p6-vh39-f01-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh39-f01-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh39-f01-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh39-f01.ps1`
## Assignment
Implement only the F01 harness scenario and focused tests as bounded subcases: unknown-extension opaque binary round-trip, empty-folder preservation, configured/default exclusion symmetry, and one cross-platform case/Unicode/invalid-name collision created only where the source platform permits it. Verify opaque bytes and empty folders round-trip; excluded content causes no one-sided deletion inference; unsafe path collisions are blocked/surfaced rather than normalized or overwritten.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field needs later filling.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `F01-filesystem-scope-and-paths.md`, VH15 evidence, current local-vault/path/exclusion/platform code, and harness fixture/verifier APIs. Respect platform containment DEC-299; do not bypass mobile DataAdapter or desktop containment to manufacture invalid paths. If a platform cannot create a specific collision safely, the relevant subcase must use the scenario's safe BLOCKED/not-applicable semantics rather than weakening the platform boundary. Do not alter frozen H0 contracts or `src/contracts/**`.

Required end state: all F01 subcases are separately evidenced, deterministic where feasible, and tests prove opaque/hash fidelity, empty-folder/exclusion behavior, and fail-closed path handling without unrelated mutation.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh39-f01.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, subcases, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release/live validation or physical F01 PASS claim.