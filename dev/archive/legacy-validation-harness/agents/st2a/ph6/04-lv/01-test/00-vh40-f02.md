# VH40 — F02 Large Transfer / Integrity / Resource Safety

Agent: `agt-ca-p6-vh40-f02-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh40-f02-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh40-f02-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh40-f02.ps1`
## Assignment
Implement only the F02 harness scenario and focused tests. Generate a deterministic harness-owned large file sized through the fixture manager to exercise the production transfer path while remaining bounded and safe; orchestrate Windows → remote → mobile, then the reverse direction with a distinct deterministic changed version. Record size, expected/observed hashes, elapsed transfer observations, and resource/concurrency evidence. Verify exact transferred hashes, no partial file exposed as final, bounded mobile-appropriate processing/concurrency, and no unrelated content mutation.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt data needs later substitution.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `F02-large-transfer-resource.md`, VH15 evidence, current transfer/local/Drive code, latency/concurrency evidence, and harness fixture/verifier APIs. Determine a bounded test-fixture sizing policy from current production limits and existing tests rather than embedding an unsafe arbitrary huge size. This build session proves harness implementation with fakes/local tests; it does not claim real iOS memory/resource evidence. Do not alter production transfer limits merely to make the scenario pass, frozen H0 contracts, or `src/contracts/**`.

Required end state: bidirectional F02 orchestration is deterministic, hashes/partial-file assertions are explicit, resource observations are captured, and focused tests cover success plus integrity mismatch/partial-final-file failure.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh40-f02.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, fixture sizing policy used in automated tests, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical F02 PASS claim.