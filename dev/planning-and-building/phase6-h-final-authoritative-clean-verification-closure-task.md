# NAME: `agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-14`

# SESSION 7 — H-FINAL — AUTHORITATIVE CLEAN VERIFICATION AND H INTEGRATION CLOSURE

## 0. AGENT IDENTITY / FINAL-SESSION BOUNDARY

You are:

`agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-14`

Repository:

`woodpk/gdrive-sync-obsidian-plugin`

Integration branch:

`phase6-sync-integration-h`

You are the **H-FINAL authoritative verification / evidence-closure agent** for the already-complete Phase 6 H synchronization integration candidate.

This is **Session 7 — H-FINAL**, the final session in the fixed remaining-session plan:

`dev/planning-and-building/phase6-h-remaining-test-failure-fix-plan.md`

All planned repair and normalization work is complete and independently supervisor-approved.

Your assignment is:

> Freeze the exact approved H-NORM source/test candidate, perform one authoritative clean verification of the complete integrated candidate, prove all frozen authorities and runtime acceptance surfaces remain exact, close H-specific evidence, append one strictly append-only H integration closure section to canonical evidence, and stop with the candidate still unmerged.

H-FINAL is **not** a repair package.

H-FINAL owns **no source or test modification**.

Do not begin Stage 3, release work, physical iPhone synchronization, or post-iPhone optimization.

---

## 1. EXACT SUPERVISOR-APPROVED INPUTS

The independently supervisor-approved H-NORM evidence-bearing H head is:

`8c831c29c7822e664d923f8cfb00544adebe1311`

The exact independently approved H-NORM **source/test candidate** is:

`cb0c81b2ddb941446f821d71274aa58af28007ec`

The H-NORM evidence head is exactly one evidence-only commit after that source/test candidate.

At the approved H-NORM boundary:

- H/V1.3 critical: `82 / 82 PASS`;
- H-I1 through H-I8: all PASS;
- adversarial G: `56 / 56 PASS`;
- V1.3 foundation: `17 / 17 PASS`, C15/C16 PASS;
- whole repository: `687 / 687 PASS`;
- fail / cancelled / skipped / todo: `0 / 0 / 0 / 0`;
- production build: PASS;
- `npm run check`: PASS;
- normalized `main.js`: `699431` bytes;
- normalized `main.js` SHA-256: `da4fbe6cb3dc704b48cba3a1d37245aca0f32a3fba9c5970ae7aab4c9ddf9482`;
- frozen authorities are unchanged;
- PR #45 is open, draft, unmerged, headed by `phase6-sync-integration-h`.

This H-FINAL tasking document is committed after the approved H-NORM evidence head as one supervisor planning-only commit.

At startup:

1. resolve live `phase6-sync-integration-h` head;
2. record it as `H_FINAL_ENTRY_HEAD`;
3. compare:
   - base: `8c831c29c7822e664d923f8cfb00544adebe1311`
   - head: `H_FINAL_ENTRY_HEAD`;
4. require that the delta contains **only**:
   - `dev/planning-and-building/phase6-h-final-authoritative-clean-verification-closure-task.md`;
5. require no `src/**`, `test/**`, contract, evidence, workflow, or other planning change.

If that entry gate is not exact, stop:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

Do not substitute a newer branch tip or alternate SHA.

---

## 2. SOURCE / TEST FREEZE

Define:

`H_FINAL_SOURCE_TEST_SHA = cb0c81b2ddb941446f821d71274aa58af28007ec`

This exact commit is the candidate under final technical review.

It is frozen.

During H-FINAL:

- do not modify any `src/**` file;
- do not modify any `test/**` file;
- do not modify package/build/runtime configuration to make verification pass;
- do not modify frozen contracts;
- do not amend/rewrite the H-NORM candidate;
- do not opportunistically patch a final-verification defect.

The proof workflow must check out **exactly** `H_FINAL_SOURCE_TEST_SHA` for all source/test/build verification.

If final verification exposes a source/test defect:

1. preserve the exact failure output;
2. do not append canonical closure;
3. do not patch the source/test candidate;
4. stop:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

There is no automatic new repair session after H-FINAL. Any defect requires explicit supervisor disposition.

---

## 3. GOVERNING MATERIAL TO READ BEFORE VERIFICATION

Read completely before execution:

1. `software-products-dev-manual-agent-led.md`
2. `dev/planning-and-building/phase6-h-remaining-test-failure-fix-plan.md`
3. this tasking document
4. `dev/evidence/_task-AGT-H-H-U6-FINAL-CLOSURE.md`
5. `dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-13.md`
6. the current complete canonical evidence file:
   - `dev/evidence/_ca-output.md`
7. the H-specific evidence lineage needed to reconstruct the exact A–G/H authority manifest actually integrated.

When older closure/task documents contain obsolete v1.2 baseline/status language, treat that old baseline/status as historical only. The exact current V1.3 authorities and current source/test SHA in this task control.

Do not invent missing A–G authority SHAs. Derive them from accepted evidence and repository history and record the exact authorities actually consumed by H.

---

## 4. FROZEN AUTHORITIES

The following must remain exact throughout H-FINAL.

### V1.3 foundation

Approved V1.3 foundation source:

`05600f7ca48a6726b72188005f29eddfc1191519`

Frozen `src/contracts/**` tree:

`0db68ced179825f929008b502335210260ca2ce3`

### Canonical evidence pre-append authority

Before H-FINAL closure append, canonical:

`dev/evidence/_ca-output.md`

must still have Git blob:

`d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`

No canonical modification is permitted until every technical verification gate has passed.

### Contract-freeze evidence

Whole-file contract-freeze blob:

`b675e0fc9776d03892a4309231b91a4bf0a84b93`

Required immutable predecessor-prefix SHA-1:

`fe527c76137b2cd578ef7050ee3444498b21a5e0`

### Semantics

Preserve:

- V1.3 separation of operational failure provenance from physical-effect certainty;
- `executionDispositionV1_3` as H/UI interpretation authority;
- all approved H-U5 production and fixture corrections;
- all G-R1/G-R2/R3 model repairs;
- H-NORM structure with no semantic change.

Any frozen-authority mismatch is an immediate blocker.

---

## 5. H-FINAL WRITE OWNERSHIP

Before all technical proof gates pass: **no tracked repository write is authorized except disposable proof-workflow infrastructure on a separate proof branch.**

After every technical gate passes, H-FINAL may create/update exactly these closure evidence surfaces on `phase6-sync-integration-h`:

1. dedicated H-FINAL evidence:
   - `dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-14.md`
2. canonical evidence, by **strict append-only EOF addition**:
   - `dev/evidence/_ca-output.md`

No other integration-branch file may change.

The final closure lineage must preserve separate auditable boundaries:

### Evidence commit 1 — H-specific final evidence

Create one evidence-only commit that changes exactly:

`dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-14.md`

Record that commit as:

`H_FINAL_SPECIFIC_EVIDENCE_HEAD`

### Evidence commit 2 — canonical append-only closure

Only after Evidence commit 1 is complete and verified, append the canonical H closure and create one second evidence-only commit changing exactly:

`dev/evidence/_ca-output.md`

Record that final commit as:

`H_FINAL_CLOSURE_HEAD`

`H_FINAL_CLOSURE_HEAD` is the final H branch head returned for independent supervisor review.

Do not combine canonical closure with source/test or planning changes.

---

## 6. AUTHORITATIVE CLEAN PROOF PROCEDURE

Use a disposable GitHub Actions proof branch.

Preferred branch:

`h-final-authoritative-clean-verification-h14`

Preferred workflow:

`.github/workflows/h-final-authoritative-clean-verification.yml`

Requirements:

- proof branch differs from the frozen source/test candidate only by proof-workflow infrastructure;
- workflow checks out exact:
  `cb0c81b2ddb941446f821d71274aa58af28007ec`;
- Node 22;
- `actions/checkout@v4` with:
  - `persist-credentials: false`
  - `fetch-depth: 0`;
- start from clean checkout;
- `npm ci` rather than reused dependency state;
- no source/test patching;
- no contract/evidence patching;
- no secret output;
- no physical Google Drive/iPhone mutation;
- no PR mutation;
- no integration-branch mutation from the proof workflow;
- preserve real command exit statuses;
- upload raw logs / deterministic proof summary artifact;
- proof branch must not be merged.

The workflow must fail if any command expected to pass returns nonzero.

Do not use `continue-on-error` to turn a technical failure green.

---

## 7. REQUIRED FINAL TECHNICAL VERIFICATION GATES

All verification is against exact:

`H_FINAL_SOURCE_TEST_SHA = cb0c81b2ddb941446f821d71274aa58af28007ec`

### Gate A — exact source/test / clean repository / immutable authorities

Prove in the authoritative job:

1. checked-out `HEAD` equals `cb0c81b2ddb941446f821d71274aa58af28007ec`;
2. tracked worktree is clean before verification;
3. `src/contracts/**` tree equals:
   `0db68ced179825f929008b502335210260ca2ce3`;
4. canonical evidence blob at the frozen source/test candidate equals:
   `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
5. contract-freeze whole-file blob equals:
   `b675e0fc9776d03892a4309231b91a4bf0a84b93`;
6. immutable predecessor-prefix SHA-1 equals:
   `fe527c76137b2cd578ef7050ee3444498b21a5e0`;
7. `git diff --check` PASS for the H-NORM normalization candidate against its approved entry authority:
   - base `f4cc17db3ec3ad356e379af04f2e000bdba0282d`
   - head `cb0c81b2ddb941446f821d71274aa58af28007ec`;
8. no proof-workflow file exists in the checked-out source/test candidate.

### Gate B — clean dependency/static verification

Run in this order:

1. `npm ci`
2. `npm run typecheck`
3. `npx tsc -p tsconfig.test.json`

Required: every command exits `0`.

Record runtime versions, including Node and npm.

### Gate C — complete repository test suite

Run the complete ordinary repository test command from the exact candidate.

Required exact result:

- total: `687`
- pass: `687`
- fail: `0`
- cancelled: `0`
- skipped: `0`
- todo: `0`
- real exit: `0`

Preserve raw TAP/test output.

### Gate D — H/V1.3 critical / integration acceptance

Run the established H/V1.3 critical regression surface explicitly.

Required exact result:

- total: `82`
- pass: `82`
- fail: `0`
- cancelled: `0`
- skipped/todo: `0`
- real exit: `0`

Explicitly prove H-I1 through H-I8 executed and PASS.

### Gate E — G adversarial runtime proof

Run the complete adversarial model explicitly even if ordinary suite discovery already includes it.

Required:

- total: `56`
- pass: `56`
- fail/cancelled/skipped/todo: all `0`
- real exit: `0`

Explicitly prove:

- all G-W1 repaired cases PASS;
- G-W2 PASS;
- G-W3 PASS.

This gate exists to make runtime execution of the adversarial model unmistakable in final closure evidence.

### Gate F — V1.3 foundation

Run the established V1.3 foundation verification explicitly.

Required:

- total: `17`
- pass: `17`
- fail/cancelled/skipped/todo: all `0`
- C15 PASS;
- C16 PASS;
- real exit: `0`.

### Gate G — build / package / mobile verifier surface

Run:

1. `npm run build`
2. `npm run check`
3. every repository-provided deterministic mobile/package verifier that `npm run check` does not already execute and that was part of the accepted Phase 6 H verification surface.

Do not invent a physical-device test.

Required:

- all applicable commands exit `0`;
- build artifact exists;
- record actual `main.js` size and SHA-256.

Expected normalized artifact identity, absent unexplained toolchain nondeterminism:

- size: `699431` bytes
- SHA-256:
  `da4fbe6cb3dc704b48cba3a1d37245aca0f32a3fba9c5970ae7aab4c9ddf9482`

If identity differs, do not automatically reject solely on byte drift. Determine whether the exact candidate/toolchain is unchanged and whether the difference is explainable/deterministic. Any unexplained production artifact drift is a blocker.

### Gate H — final integration-branch / PR safety state before evidence closure

Before writing H-FINAL evidence, verify read-only that:

- `phase6-sync-integration-h` contains no source/test change after `cb0c81b2ddb941446f821d71274aa58af28007ec`;
- the only post-candidate changes before H-FINAL evidence are the approved H-NORM evidence commit and this H-FINAL planning-only commit;
- PR #45 is:
  - state `open`;
  - `draft = true`;
  - `merged = false`;
  - head branch `phase6-sync-integration-h`;
- no merge into `phase6-integration`, `master`, or `main` occurred;
- no proof workflow is present on the integration branch.

If any technical gate A–H fails, do not write canonical closure.

---

## 8. REQUIRED AUTHORITATIVE PROOF METADATA

After the final proof run completes, independently cross-check GitHub metadata rather than relying only on values copied from workflow text.

Record in H-FINAL evidence:

- proof branch;
- workflow path;
- proof workflow-only head commit;
- exact checked-out source/test candidate;
- run ID;
- job ID;
- workflow/job conclusion;
- artifact ID;
- artifact name;
- artifact digest;
- artifact size;
- raw/summary artifact contents supporting the reported totals;
- exact Node/npm versions;
- `main.js` size/hash.

Download/inspect the authoritative artifact when necessary to reconcile proof-summary values with GitHub artifact metadata.

Do not repeat the P9/G-R1 evidence-provenance mistakes. Every identifier must be cross-checked before evidence is committed.

---

## 9. INTEGRATED A–G / H AUTHORITY MANIFEST

The dedicated H-FINAL evidence must reconstruct the exact integrated authority lineage from accepted evidence and repository history.

At minimum identify, without guessing:

- frozen V1.3 foundation/adoption authority;
- A approved V1.3 candidate/evidence authority actually integrated;
- B approved V1.3 candidate/evidence authority actually integrated;
- C accepted authority represented in the assembled H candidate;
- D approved V1.3 candidate/evidence authority actually integrated;
- E accepted authority represented in the assembled H candidate;
- F accepted authority represented in the assembled H candidate;
- G final approved repaired candidate/evidence authority;
- H integration candidate and H-U5/H-NORM correction lineage necessary to explain the final source/test state.

Use existing accepted evidence/commits as the source of truth.

Do not make the canonical closure section a giant historical transcript. Put the detailed authority manifest in the dedicated H-FINAL evidence file and summarize the key final authorities in canonical closure.

---

## 10. H-SPECIFIC FINAL EVIDENCE

Only after all technical gates succeed, create:

`dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-14.md`

It must record at minimum:

1. agent/session identity;
2. approved H-NORM evidence head:
   `8c831c29c7822e664d923f8cfb00544adebe1311`;
3. `H_FINAL_ENTRY_HEAD` and exact planning-only entry proof;
4. frozen `H_FINAL_SOURCE_TEST_SHA`:
   `cb0c81b2ddb941446f821d71274aa58af28007ec`;
5. proof that no source/test changed after that SHA;
6. complete integrated A–G/H authority manifest derived from accepted evidence;
7. proof workflow branch/path/head;
8. run/job/artifact metadata cross-checked against GitHub;
9. exact command list and real exits;
10. Node/npm versions;
11. whole repository `687 / 687` result;
12. H/V1.3 critical `82 / 82`, H-I1–H-I8 PASS;
13. adversarial `56 / 56`, including G-W1/W2/W3 PASS;
14. V1.3 foundation `17 / 17`, C15/C16 PASS;
15. build/check/mobile-package verification;
16. `main.js` size/hash;
17. frozen contract-tree verification;
18. canonical pre-append blob verification;
19. contract-freeze blob/prefix verification;
20. PR #45 pre-closure state;
21. explicit statement that there are zero residual automated failures/cancellations/skips/todos;
22. explicit statement that source/tests were frozen and no H-FINAL repair was performed;
23. explicit statement that no physical synchronization was executed;
24. explicit statement that Stage 3/release work was not begun;
25. explicit statement that the candidate remains unmerged.

Commit/push this as **Evidence commit 1** changing only that file.

Record:

`H_FINAL_SPECIFIC_EVIDENCE_HEAD`

Verify the commit manifest before proceeding to canonical closure.

---

## 11. CANONICAL EVIDENCE STRICT APPEND-ONLY CLOSURE

Only after:

- all technical gates PASS;
- H-specific final evidence is committed;
- the H-specific evidence commit manifest is verified;

may you append one H integration closure section to:

`dev/evidence/_ca-output.md`

### 11.1 Pre-append invariant

Immediately before the append, prove the canonical file's existing historical content still has Git blob:

`d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`

Capture the exact pre-append bytes/hash.

If the file differs, stop:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

### 11.2 True EOF append

Perform a true append operation to the checked-out file (for example `cat >>` / equivalent byte-preserving append), not a formatter, renderer, regeneration, normalization, or rewrite of historical content.

Do not reflow existing Markdown.

Do not trim trailing whitespace/newlines from the historical prefix.

Do not change line endings or encoding of the existing prefix.

### 11.3 Required canonical closure content

Append one concise section titled clearly as the Phase 6 H integration closure and include:

- final frozen source/test SHA:
  `cb0c81b2ddb941446f821d71274aa58af28007ec`;
- approved H-NORM evidence head:
  `8c831c29c7822e664d923f8cfb00544adebe1311`;
- H-FINAL proof run/job/artifact identifiers;
- whole repository: `687 / 687 PASS`;
- H/V1.3 critical: `82 / 82 PASS`, H-I1–H-I8 PASS;
- adversarial G: `56 / 56 PASS`;
- V1.3 foundation: `17 / 17 PASS`, C15/C16 PASS;
- build/check PASS and final `main.js` size/hash;
- frozen `src/contracts/**` tree:
  `0db68ced179825f929008b502335210260ca2ce3`;
- statement that canonical historical content was preserved as a byte-identical prefix;
- statement that source/test was frozen during H-FINAL;
- statement that PR #45 remains open/draft/unmerged;
- statement that the H candidate was **not merged** and Stage 3/physical iPhone validation did not begin.

Do not duplicate the full detailed authority manifest here; point to the dedicated H-FINAL evidence file for detailed lineage.

### 11.4 Post-append integrity proof

Before committing canonical closure, prove:

1. the complete pre-append canonical bytes are a byte-identical prefix of the post-append file;
2. the canonical diff consists only of EOF additions;
3. there are zero deletions;
4. there is no historical hunk modification;
5. the new H-FINAL closure heading/marker appears exactly once;
6. `git diff --check` passes for the canonical append.

Commit as **Evidence commit 2**, changing exactly:

`dev/evidence/_ca-output.md`

Record resulting final head:

`H_FINAL_CLOSURE_HEAD`

If append-only integrity cannot be demonstrated, do not claim completion.

---

## 12. FINAL POST-CLOSURE READ-ONLY AUDIT

After `H_FINAL_CLOSURE_HEAD` is pushed, perform one final read-only audit.

Prove:

1. branch head equals `H_FINAL_CLOSURE_HEAD`;
2. `8c831c29c7822e664d923f8cfb00544adebe1311` → final head changes exactly:
   - this H-FINAL tasking file;
   - dedicated H-FINAL evidence file;
   - canonical `_ca-output.md` append;
3. no `src/**` or `test/**` change exists in that delta;
4. source/test content remains that of `cb0c81b2ddb941446f821d71274aa58af28007ec`;
5. frozen contracts remain exact;
6. canonical append-only prefix proof remains valid at the committed final blob;
7. proof workflow remains absent from integration branch;
8. PR #45 remains:
   - open;
   - draft;
   - unmerged;
   - headed by `phase6-sync-integration-h` at `H_FINAL_CLOSURE_HEAD`;
9. no merge/release/Stage 3/physical-device action occurred.

This final audit is read-only. Do not create another cleanup commit unless the closure evidence itself is objectively defective; if so, stop for supervisor decision rather than silently adding another closure wave.

---

## 13. COMPLETION RESPONSE

Return a concise but complete final report containing:

- `H_FINAL_ENTRY_HEAD`;
- `H_FINAL_SOURCE_TEST_SHA`;
- `H_FINAL_SPECIFIC_EVIDENCE_HEAD`;
- `H_FINAL_CLOSURE_HEAD`;
- exact post-H-NORM changed-file manifest;
- authoritative proof branch/workflow/head/run/job/artifact metadata;
- Node/npm versions;
- whole repository totals;
- H/V1.3 critical totals + H-I1–H-I8;
- G adversarial totals + G-W1/W2/W3;
- V1.3 foundation totals + C15/C16;
- build/check/mobile-package status;
- final `main.js` size/hash;
- frozen-authority verification;
- canonical pre/post append proof and exact append-only manifest;
- PR #45 final state/head;
- confirmation source/test remained frozen;
- confirmation there are no residual automated failures;
- confirmation no physical synchronization, Stage 3, release, or merge occurred;
- blockers, if any.

The final line must be exactly:

`PHASE 6 H INTEGRATION CANDIDATE COMPLETE — READY FOR INDEPENDENT SUPERVISOR REVIEW — NOT MERGED`

If any technical verification, frozen-authority, evidence-provenance, or append-only requirement fails, do not emit the completion line. End instead:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

---

## 14. HARD STOP

After successful H-FINAL closure:

- stop;
- do not merge PR #45;
- do not merge to `phase6-integration`;
- do not modify `main` or `master`;
- do not begin Stage 3;
- do not create a release;
- do not begin post-iPhone optimization;
- do not conduct physical iPhone synchronization.

The next action after this agent returns is **independent supervisor review of the completed H integration candidate**.