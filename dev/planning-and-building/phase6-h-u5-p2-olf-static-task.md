# AGENT NAME: `agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-04`

## 0. AGENT IDENTITY

You are:

`agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-04`

You are the next serial Phase 6 H integration agent for:

`woodpk/gdrive-sync-obsidian-plugin`

You are the successor to:

`agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-03`

This is a continuation assignment. Do not restart H integration, H-U4 classification, V1.3 A/B/D integration, H-C1 proof restoration, or the completed H-U5-P1 fixture package.

Your assignment is exactly one bounded H-U5 continuation unit:

`H-U5-P2 — OLF-STATIC acceptance-map fixture correction`

Do not begin another H-U5 package after completing this one.

Do not begin H-FINAL.

---

# 1. REPOSITORY / BRANCH / CURRENT AUTHORITY

Repository:

`woodpk/gdrive-sync-obsidian-plugin`

Writable integration branch:

`phase6-sync-integration-h`

Supervisor-approved H branch head immediately before this tasking file was added:

`ac6afd51b2eb23fd94cecaa7e502735e38e93d22`

Retained V1.3 A/B/D implementation authority:

`4fef16f498dafba15fc1da5a63124567c5f56bcc`

Frozen V1.3 `src/contracts/**` tree:

`0db68ced179825f929008b502335210260ca2ce3`

Canonical evidence blob that must remain unchanged:

`d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`

Current H verification PR:

`#45`

Required PR state throughout this unit:

`OPEN / DRAFT / UNMERGED`

The tasking commit that created this file necessarily advances the branch above `ac6afd51...`. Therefore do not require the live branch head itself to equal `ac6afd51...` at entry. Instead:

1. record the actual live branch head as `H_U5_P2_ENTRY_HEAD`;
2. compare `ac6afd51b2eb23fd94cecaa7e502735e38e93d22...H_U5_P2_ENTRY_HEAD`;
3. require that the only post-authority repository change is this supervisor tasking file under `dev/planning-and-building/**` unless the supervisor has explicitly supplied another approved planning/evidence-only commit;
4. if any unapproved `src/**` or `test/**` change appears above `ac6afd51...`, stop with `BLOCKED — SUPERVISOR DECISION REQUIRED`.

Do not substitute another SHA for any authority named above.

---

# 2. COMPLETED WORK THAT MUST BE PRESERVED

The following work is complete and must not be reopened in this unit.

## H-U1 through H-U4

The A–G integration, H production composition, H mutation/restart integration tests, H feed/lifecycle/merge integration, G runtime discovery, D-C13 convergence correction consumption, and H-U4 repository-wide failure classification are complete.

H-U4 established:

- `H-INTEGRATION-DEFECT`: `0`;
- all remaining non-G failures were obsolete legacy fixture/evidence constructions;
- G-owned failures remained worker-owned and are not H repair authority.

## V1.3 A/B/D integration

The V1.3 A/B/D integration candidate is approved and retained.

Do not modify or reinterpret:

- A's approved Drive failure provenance semantics;
- B's approved local failure provenance semantics;
- D's approved orchestration failure provenance semantics;
- H's V1.3 product-surface disposition mapping;
- the frozen V1.3 contract helper `executionDispositionV1_3`.

H-C1 candidate-proof authority restoration is closed.

## H-U5-P1

The first obsolete physical-controller fixture package is complete/preserved.

Do not redo or broaden work in:

- `test/phase5-controller.test.ts`;
- `test/phase5-group-a-recovery-state.test.ts`;
- `test/phase5-group-b-scope-transfer.test.ts`.

Do not reinterpret their prior H-U4 failures as current authority requiring another repair.

---

# 3. SOURCE OF THIS UNIT — APPROVED H-U4 CLASSIFICATION

H-U4 classified one independent stale acceptance-map failure as:

`OBSOLETE-LEGACY-FIXTURE / OLF-STATIC`

The affected test is:

`test/phase5-acceptance-map.test.ts`

H-U4 failure:

`Phase5 acceptance map has exact source-verified executable evidence for scenarios 1 through 50`

The exact stale row is scenario 31.

Current stale acceptance-map literal:

`Phase5 scenario 31 local-change debounce coalesces repeated events into one automatic pass`

Current live executable scheduler test name:

`Phase5 scenario 31 local-change debounce coalesces repeated events into one scheduler-owned automatic pass`

The scheduler test itself is valid and passes. The defect is only that the acceptance-map evidence literal was not updated when scheduler ownership wording changed.

This is a static evidence-fixture mismatch.

It is not a scheduler defect.

It is not a production synchronization defect.

It is not a V1.3 contract defect.

It is not a G worker defect.

---

# 4. EXACT AUTHORIZED REPAIR

This unit authorizes exactly one test-source modification:

`test/phase5-acceptance-map.test.ts`

Change only scenario 31's `testName` value so it exactly matches the current executable scheduler test name:

`Phase5 scenario 31 local-change debounce coalesces repeated events into one scheduler-owned automatic pass`

Preserve:

- scenario number `31`;
- scenario meaning;
- `testFile` = `test/phase5-scheduler-acceptance.test.ts`;
- orchestration meaning;
- every other acceptance-map row;
- the acceptance-map source-verification behavior;
- the actual scheduler test unchanged.

Do not weaken the acceptance-map verifier.

Do not replace exact source matching with fuzzy matching.

Do not rename the live scheduler test merely to make the stale map pass.

Do not change production source.

Do not modify any other test file.

If any other repository file is proven necessary for the semantic repair, stop before editing it and report:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

---

# 5. ENTRY INSPECTION — READ ONLY

Before editing:

1. confirm the live branch is `phase6-sync-integration-h` and record `H_U5_P2_ENTRY_HEAD`;
2. compare the approved pre-task authority `ac6afd51b2eb23fd94cecaa7e502735e38e93d22` to the live entry head and verify only authorized tasking/planning history was added;
3. verify `src/contracts/**` still resolves to tree `0db68ced179825f929008b502335210260ca2ce3`;
4. verify canonical `dev/evidence/_ca-output.md` still resolves to blob `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
5. fetch and inspect:
   - `test/phase5-acceptance-map.test.ts`;
   - `test/phase5-scheduler-acceptance.test.ts`;
   - the H-U4 evidence containing classification key `OLF-STATIC`;
6. confirm the stale literal and live executable name are exactly the strings stated in this task.

Do not execute the full repository just to rediscover the H-U4 defect before editing. H-U4 already established the before-state classification.

---

# 6. IMPLEMENTATION PROCEDURE

Modify only:

`test/phase5-acceptance-map.test.ts`

After the correction is committed/pushed on `phase6-sync-integration-h`, record the resulting SHA as:

`H_U5_P2_CANDIDATE_SHA`

Then compare:

`H_U5_P2_ENTRY_HEAD...H_U5_P2_CANDIDATE_SHA`

Acceptance:

- exactly one changed file;
- that file is `test/phase5-acceptance-map.test.ts`;
- the diff is limited to the scenario-31 stale test-name literal unless a mechanically necessary line-format change is unavoidable;
- no `src/**` file changes;
- no other `test/**` file changes;
- no tasking/evidence file changes in the implementation commit.

If the candidate delta is broader, correct the scope before verification.

---

# 7. EXECUTION ARCHITECTURE

Do not spend the turn building a local repository execution environment.

Use repository reads/writes for inspection and the bounded source change.

Use one disposable GitHub Actions proof branch for execution.

Do not modify the repository's persistent production CI workflow.

Do not use Replit, Codespaces, or another external execution environment.

Do not enter repeated workflow-repair loops.

If GitHub execution capability is unavailable, stop with:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

---

# 8. DISPOSABLE PROOF BRANCH

Create one disposable proof branch from exactly:

`H_U5_P2_CANDIDATE_SHA`

Preferred branch name:

`h-u5-p2-olf-static-proof-h04`

If the name exists, use a unique successor suffix. Never force-update or overwrite an existing branch.

On the disposable branch only, add:

`.github/workflows/h-u5-p2-olf-static-proof.yml`

The workflow must explicitly check out `H_U5_P2_CANDIDATE_SHA` and use Node 22.

The proof workflow must not patch production/test source and must never be merged into `phase6-sync-integration-h`.

---

# 9. REQUIRED VERIFICATION

Run the following in one bounded proof workflow.

## A. Exact candidate / invariants before execution

Verify:

- `git rev-parse HEAD == H_U5_P2_CANDIDATE_SHA`;
- `src/contracts/**` tree = `0db68ced179825f929008b502335210260ca2ce3`;
- canonical `dev/evidence/_ca-output.md` blob = `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
- tracked working tree clean before test execution.

Print Node/npm versions.

## B. Dependency and static gates

Run directly:

`npm ci`

`npm run typecheck`

`npx tsc -p tsconfig.test.json`

`git diff --check H_U5_P2_ENTRY_HEAD...H_U5_P2_CANDIDATE_SHA`

All must return exit code `0`.

## C. Focused OLF-STATIC verification

Run exactly the compiled acceptance map and its live scheduler evidence file:

`node --test .test-build/test/phase5-acceptance-map.test.js .test-build/test/phase5-scheduler-acceptance.test.js`

Capture the real process exit code.

Acceptance:

- exit code `0`;
- acceptance-map test passes;
- scenario-31 live scheduler test passes;
- no fail/cancelled/skipped/todo result in this focused command.

## D. V1.3 / H critical-regression verification

Run the current compiled V1.3 foundation and consolidated H integration entrypoint so this evidence-only fixture change cannot hide integration drift:

`node --test .test-build/test/phase6-foundation-failure-provenance.test.js .test-build/test/phase6-h-sync-integration.test.js`

Capture the real process exit code.

Acceptance:

- exit code `0` for the H/V1.3 acceptance surface that is expected to be green at the retained approved candidate;
- H integration markers remain green;
- no production behavior was changed by P2.

If this command includes inherited G worker failures due to the consolidated H entrypoint's G import, do not repair G. Record the exact raw outcome and distinguish H/V1.3 pass markers from G-owned failures. Do not weaken or remove G discovery to manufacture a green command.

## E. Current whole-repository measurement

Run:

`npm test`

directly, with no `tee` masking and no shell pipeline that can overwrite the real exit code.

Capture the numeric process exit code immediately.

Important:

Do **not** assume the old H-U4 aggregate `656 / 584 / 47 / 25` is still the current V1.3/P1 aggregate. The branch has legitimately advanced since that V1.2 classification run.

This run is a measurement gate, not authorization for another repair.

Record:

- tests;
- pass;
- fail;
- cancelled;
- skipped;
- todo;
- real npm exit code.

The only semantic expectation for this P2 package is that the stale scenario-31 acceptance-map failure is gone. All other non-pass results remain outside this unit unless they are direct evidence that the one-line repair itself caused regression.

If unexpected new non-pass behavior appears, do not broaden the repair. Record it and stop for supervisor review.

## F. Build / package gate

Run:

`npm run build`

Require exit code `0` and capture the existing build-verifier results.

## G. Final invariant recheck

After all commands, verify again:

- HEAD is still `H_U5_P2_CANDIDATE_SHA`;
- tracked source/test working tree is clean;
- `src/contracts/**` tree remains `0db68ced179825f929008b502335210260ca2ce3`;
- canonical evidence blob remains `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`.

Upload the proof logs, exit-code record, aggregate counts, and invariant results as one GitHub Actions artifact.

---

# 10. NO REPAIR SPIN

This unit is intentionally tiny.

The expected implementation correction is one stale literal.

Do not turn P2 into an exploratory refactor.

If the proof workflow definition itself has one bounded mechanical mistake that prevents the intended commands from running, correct only that disposable workflow and rerun once.

If source/test verification reveals a new semantic defect outside the authorized acceptance-map line, stop and report it.

Do not begin a second repair cycle against unrelated repository failures.

---

# 11. EVIDENCE CLOSURE

After the P2 candidate is verified, create or append only the successor H-04 evidence record:

`dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-04.md`

Do not modify predecessor H-01, H-02, or H-03 evidence files.

Do not modify canonical:

`dev/evidence/_ca-output.md`

Record:

- agent identity;
- `H_U5_P2_ENTRY_HEAD`;
- `H_U5_P2_CANDIDATE_SHA`;
- classification `OBSOLETE-LEGACY-FIXTURE / OLF-STATIC`;
- exact stale and corrected scenario-31 strings;
- confirmation only `test/phase5-acceptance-map.test.ts` changed in the implementation candidate;
- focused OLF-STATIC proof counts and real exit code;
- V1.3/H critical-regression results and any inherited G qualification;
- current full-repository measured totals and real npm exit code;
- typecheck/test-compile/build/diff results;
- proof run ID/job ID;
- proof artifact ID/digest;
- frozen-contract result;
- canonical-evidence result;
- PR #45 state;
- disposable proof-branch state;
- explicit statement that H-U5-P3 and H-FINAL were not started.

Commit/push only that evidence file after the verified candidate and record the resulting SHA as:

`H_U5_P2_EVIDENCE_SHA`

Then verify:

- `H_U5_P2_ENTRY_HEAD...H_U5_P2_CANDIDATE_SHA` = only `test/phase5-acceptance-map.test.ts`;
- `H_U5_P2_CANDIDATE_SHA...H_U5_P2_EVIDENCE_SHA` = only the H-04 evidence file;
- frozen contracts unchanged;
- canonical evidence unchanged;
- PR #45 still OPEN / DRAFT / UNMERGED.

---

# 12. REMAINING WORK IS NOT AUTHORIZED IN P2

After this one OLF-STATIC repair, known H-U4-classified work still remains.

Do not repair any of it in this unit.

Specifically do not repair:

- remaining `OLF-PHYSICAL` Phase 5 Group-D fixture families;
- any `OLF-FAKE-AUTH` fixture family;
- any G-owned `G-W1`, `G-W2`, or `G-W3` defect;
- any production source;
- any V1.3 contract;
- any H integration code.

A later supervisor task will select the next causal package.

H-FINAL remains unauthorized.

---

# 13. HARD BOUNDARIES

Do not:

- restart H-U4;
- redo H-U5-P1;
- modify production code;
- modify `src/contracts/**`;
- modify `test/phase5-scheduler-acceptance.test.ts`;
- modify any test other than `test/phase5-acceptance-map.test.ts` in the implementation candidate;
- modify canonical `dev/evidence/_ca-output.md`;
- modify predecessor H evidence files;
- modify worker branches;
- modify `phase6-integration`, `main`, or `master`;
- merge PR #45;
- merge the disposable proof branch;
- repair G;
- begin H-U5-P3;
- begin H-FINAL;
- perform physical synchronization.

---

# 14. COMPLETION REPORT / HARD STOP

On completion report only:

1. `H_U5_P2_ENTRY_HEAD`;
2. `H_U5_P2_CANDIDATE_SHA`;
3. `H_U5_P2_EVIDENCE_SHA`;
4. implementation files changed;
5. exact scenario-31 correction;
6. focused proof run ID/job ID and result;
7. V1.3/H regression result;
8. current full-suite real exit code and totals;
9. typecheck/test-compile/build/diff results;
10. proof artifact ID/digest;
11. frozen-contract result;
12. canonical-evidence result;
13. PR #45 state;
14. disposable proof-branch state;
15. explicit remaining classified families not touched.

Then end exactly:

`H-U5-P2 COMPLETE — READY FOR SUPERVISOR REVIEW — DO NOT START H-U5-P3 OR H-FINAL`

If the task cannot be completed without changing anything outside the one authorized test file:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

If turn capacity ends after a safe committed state:

`TURN CAPACITY EXHAUSTED / CONTINUATION REQUIRED — H-U5-P2 NOT COMPLETE`
