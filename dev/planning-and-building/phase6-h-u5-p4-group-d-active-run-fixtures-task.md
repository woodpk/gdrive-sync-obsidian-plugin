# AGENT NAME: `agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-06`

## 0. AGENT IDENTITY

You are:

`agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-06`

You are the next serial Phase 6 H integration agent for:

`woodpk/gdrive-sync-obsidian-plugin`

You are the successor to:

`agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-05`

This is a continuation assignment.

Do not restart H integration.
Do not restart H-U4 classification.
Do not redo V1.3 A/B/D integration or H-C1 authority restoration.
Do not redo H-U5-P1, H-U5-P2, or H-U5-P3.

Your assignment is exactly one bounded H-U5 continuation unit:

`H-U5-P4 — OLF-PHYSICAL Phase 5 Group-D active-run fixture modernization`

Do not begin another H-U5 package after completing this one.

Do not begin the separately planned production-structure normalization pass.
Do not begin H-FINAL / H-U6 final closure.

---

# 1. REPOSITORY / BRANCH / CURRENT AUTHORITY

Repository:

`woodpk/gdrive-sync-obsidian-plugin`

Writable integration branch:

`phase6-sync-integration-h`

Supervisor-approved H-U5-P3 final evidence/branch head immediately before this tasking file is added:

`8eab8ac2407a35609e77fd9ebc4c54bda349191c`

H-U5-P3 fixture-modernization SHA:

`d48be2891b0527cc9950a09d2cc5f35920293a87`

H-U5-P3 supervisor-authorized production-correction / final verified candidate SHA:

`a21d09dc295667190b6197d8b66e6f8864e3ce19`

Retained V1.3 A/B/D implementation authority:

`4fef16f498dafba15fc1da5a63124567c5f56bcc`

Frozen V1.3 `src/contracts/**` tree:

`0db68ced179825f929008b502335210260ca2ce3`

Canonical evidence blob that must remain unchanged:

`d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`

Restored contract-freeze whole-file blob:

`b675e0fc9776d03892a4309231b91a4bf0a84b93`

Required immutable predecessor prefix hash for the first 16,296 bytes of `dev/planning-and-building/phase6-sync-contract-freeze.md`:

`fe527c76137b2cd578ef7050ee3444498b21a5e0`

Current H verification PR:

`#45`

Required PR state throughout this unit:

`OPEN / DRAFT / UNMERGED`

The supervisor tasking commit that creates this file necessarily advances the branch above `8eab8ac...`.

At entry:

1. record the actual live branch head as `H_U5_P4_ENTRY_HEAD`;
2. compare `8eab8ac2407a35609e77fd9ebc4c54bda349191c...H_U5_P4_ENTRY_HEAD`;
3. require that the only post-approval change is this tasking file under `dev/planning-and-building/**` unless the supervisor explicitly supplied another approved planning/evidence-only commit;
4. if any unapproved `src/**` or `test/**` change appears above `8eab8ac...`, stop with `BLOCKED — SUPERVISOR DECISION REQUIRED`.

Do not substitute another SHA for any authority named above.

---

# 2. COMPLETED WORK THAT MUST BE PRESERVED

## H-U1 through H-U4

H production composition, mutation/restart integration, feed/lifecycle/merge integration, full verification, and failure classification are complete.

Accepted H-U4 classification established:

- `H-INTEGRATION-DEFECT`: `0` at the accepted H-U4 classification point;
- obsolete legacy fixtures are repairable only in bounded H-U5 packages;
- G adversarial-model failures remain worker-owned and are not H repair authority.

## H-U5-P1

The foundational `OLF-PHYSICAL` constructor-fixture modernization package is complete.

Preserve and use as references where appropriate:

- `test/phase5-controller.test.ts`;
- `test/phase5-group-a-recovery-state.test.ts`;
- `test/phase5-group-b-scope-transfer.test.ts`.

## H-U5-P2

The `OLF-STATIC` scenario-31 acceptance-map correction and the separately authorized contract-freeze provenance restoration are complete.

Do not modify either surface.

## H-U5-P3

The Group-D first-sync fixture modernization is complete and supervisor-approved.

P3 also exposed and repaired one genuine H-owned production defect in completed-intent semantic-authority rebasing under separate supervisor authorization.

Preserve:

- `test/phase5-group-d-first-sync-integration.test.ts` at the approved P3 state;
- `src/product/phase6-sync-integration.ts` at the approved P3 production-correction state;
- all P3 assertions and semantics.

Use the P3 fixture as a current implementation reference for:

- `IntegratedSynchronizationStateStore`;
- explicit hardened remote physical mutation seams;
- explicit local transactional mutation seams;
- state/authority construction that preserves current fail-closed production behavior.

Do not copy P3 code mechanically if this file requires less machinery.

---

# 3. CURRENT VERIFIED REGRESSION BASELINE

The supervisor-approved P3 proof established the current branch behavior before this P4 tasking commit.

## H/V1.3 critical gate

`82 total / 69 pass / 13 fail / 0 cancelled / 0 skipped / 0 todo`

All 17 V1.3 foundation tests pass, including C15/C16.
All H-I1 through H-I8 markers pass.
All 13 failures are the already-classified G-owned adversarial-model family.

P4 must not change that failure family.

## Whole repository

Current verified full-suite baseline:

- tests: `687`
- pass: `631`
- fail: `31`
- cancelled: `25`
- skipped: `0`
- todo: `0`
- real `npm test` exit code: `1`

This is the authoritative bookkeeping baseline for P4.

---

# 4. SOURCE OF THIS UNIT — H-U4 OLF-PHYSICAL + FRESH P3 MEASUREMENT

H-U4 classified the affected Group-D active-run fixture family under:

`OBSOLETE-LEGACY-FIXTURE / OLF-PHYSICAL`

Authorized file:

`test/phase5-group-d-active-run-integration.test.ts`

The current file contains three scenarios:

1. `G2 scenario 15 one properly attested ordinary deletion trashes only the remote copy without triggering bulk approval`
2. `G2 scenario 24 stale operation precondition refuses mutation, completes with attention, and awaits an external reconciliation trigger`
3. `G2 scenario 25 remote change during an active production run is deferred to the later serialized Changes reconciliation`

Fresh P3 full-suite evidence at the approved candidate confirms the current state:

- scenario 15: **FAIL** — reviewed execution returns `rejected` where the fixture expects `accepted`;
- scenario 24: **PASS**;
- scenario 25: **CANCELLED** — its update-start promise remains pending because hardened production authority never reaches the legacy raw mutation path;
- focused family therefore has `3 total / 1 pass / 1 fail / 1 cancelled` in the current full-suite run.

The historical H-U4 classification and the fresh P3 run are causally consistent: the shared active-run harness still constructs the controller/executor with pre-hardening raw mutation/state assumptions.

---

# 5. CAUSAL DEFECT TO REPAIR

The current shared helper in `test/phase5-group-d-active-run-integration.test.ts` still constructs:

- direct `PersistentSynchronizationStateStore` usage;
- `ProductSnapshotAssembler` over that raw store;
- `ProductSynchronizationExecutor` over raw local/Drive callbacks;
- `IntegratedProductController` without the current explicit writable synchronization-authority seam;
- `IntegratedProductController` without the current hardened reliable remote mutation and local transactional mutation seams required for physical effects.

That construction is stale relative to current production behavior.

Production is required to fail closed when authoritative/recoverable physical mutation seams are absent.

Consequently:

- scenario 15 is rejected before its intended single remote trash can execute;
- scenario 25 never reaches the legacy `drive.update` callback that resolves the fixture's `updateStarted` promise, causing cancellation;
- scenario 24 still passes because it intentionally proves a stale precondition refuses physical mutation before that missing physical authority would be exercised.

The fixture must be modernized; production fail-closed semantics must not be weakened.

---

# 6. EXACT AUTHORIZED REPAIR

This unit initially authorizes exactly one implementation/test file modification:

`test/phase5-group-d-active-run-integration.test.ts`

Modernize only that file's shared construction and minimal in-memory physical-effect bookkeeping so all three existing scenarios execute against the current hardened controller/executor authority model.

You may:

- adjust imports in this file;
- replace direct raw synchronization-state construction with the existing `IntegratedSynchronizationStateStore` over the durable backing store;
- seed any currently required synchronization/base/path authority metadata that the three existing scenarios legitimately require;
- provide a minimal in-file current reliable remote mutation port for only the operations actually exercised by these scenarios;
- provide a minimal in-file `LocalTransactionalMutationPort` only if required by the controller's current mandatory seam or by these scenarios' actual behavior;
- preserve or minimally adapt the existing in-memory maps/counters/promises needed to assert remote trash, stale refusal, active-run serialization, remote change consumption, local reconciliation, and cursor advancement;
- use the approved P1/P3 fixture-modernization patterns as references.

For scenario 15, the hardened remote trash seam must preserve the scenario's existing semantic assertion: exactly the properly attested ordinary deletion is trashed and no bulk-deletion approval is triggered.

For scenario 24, preserve its current passing behavior and assertions. Do not turn stale-precondition refusal into mutation or automatic self-reconciliation.

For scenario 25, preserve the real active-run sequencing behavior:

1. the first run begins a legitimate remote update through the hardened production path;
2. the fixture pauses that physical update at the same semantic point currently represented by `updateStarted` / release control;
3. a remote change arrives while the production run is active;
4. `noteChangeDuringRun()` records the deferred trigger;
5. the first run completes;
6. the later serialized Changes reconciliation consumes the deferred remote change;
7. local `b.md` converges to `B1`;
8. remote `a.md` remains `A1`;
9. the durable cursor advances to the expected later cursor.

Do not:

- modify production code in the initial P4 repair;
- modify `src/contracts/**`;
- modify another test file;
- weaken, delete, skip, rename away, or disable any of the three tests;
- bypass the hardened physical execution path with assertion-only mocks;
- reintroduce legacy raw mutation fallback into production;
- change scheduler/run serialization semantics;
- change stale-precondition semantics;
- change remote-change cursor semantics;
- change bulk-deletion approval semantics;
- change durable state/BASE semantics;
- change the P3 production correction.

If modernizing the fixture correctly exposes a genuine production-side semantic defect, do not hide it and do not edit production code without supervisor authority. Stop and report:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

Your blocker report must identify the exact production surface, causal mismatch, observed state transition, and why fixture-only correction would mask real production behavior.

---

# 7. ENTRY INSPECTION — READ ONLY

Before editing:

1. confirm the live branch and record `H_U5_P4_ENTRY_HEAD`;
2. verify the post-`8eab8ac...` delta is tasking/planning-only as specified above;
3. verify `src/contracts/**` tree remains exactly `0db68ced179825f929008b502335210260ca2ce3`;
4. verify canonical `dev/evidence/_ca-output.md` remains blob `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
5. verify `dev/planning-and-building/phase6-sync-contract-freeze.md` remains whole-file blob `b675e0fc9776d03892a4309231b91a4bf0a84b93` and first-16,296-byte prefix hash `fe527c76137b2cd578ef7050ee3444498b21a5e0`;
6. inspect in full:
   - `test/phase5-group-d-active-run-integration.test.ts`;
   - current approved `test/phase5-group-d-first-sync-integration.test.ts` as the closest Group-D hardened-fixture reference;
   - current approved `test/phase5-controller.test.ts` as the foundational constructor-fixture reference;
   - the current controller/executor constructor surfaces required to understand authority and physical mutation dependencies;
   - the P3 production correction only as needed to avoid regressing it.

Do not run the full repository merely to rediscover the known current P4 baseline before editing.

---

# 8. IMPLEMENTATION PROCEDURE

Modify only:

`test/phase5-group-d-active-run-integration.test.ts`

After the fixture correction is committed/pushed on `phase6-sync-integration-h`, record the resulting SHA as:

`H_U5_P4_CANDIDATE_SHA`

Then compare:

`H_U5_P4_ENTRY_HEAD...H_U5_P4_CANDIDATE_SHA`

Initial acceptance:

- exactly one changed file;
- that file is `test/phase5-group-d-active-run-integration.test.ts`;
- no `src/**` changes;
- no other `test/**` changes;
- no planning/evidence changes in the implementation commit.

If the candidate delta is broader, correct the scope before verification.

If a production defect is exposed, stop for supervisor authority rather than broadening the candidate yourself.

---

# 9. EXECUTION ARCHITECTURE

Do not spend the turn building or repairing a local repository execution environment.

Use repository reads/writes for inspection and the bounded fixture change.

Use one disposable GitHub Actions proof branch for execution.

Do not modify the repository's persistent production CI workflow.
Do not use Replit, Codespaces, or another external execution environment.
Do not enter repeated workflow-repair loops.

If GitHub execution capability is unavailable, stop with:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

---

# 10. DISPOSABLE PROOF BRANCH

Create one disposable proof branch from exactly:

`H_U5_P4_CANDIDATE_SHA`

Preferred branch name:

`h-u5-p4-group-d-active-run-proof-h06`

If that name exists, use a unique successor suffix. Never force-update or overwrite an existing branch.

On the disposable branch only, add:

`.github/workflows/h-u5-p4-group-d-active-run-proof.yml`

The workflow must explicitly check out `H_U5_P4_CANDIDATE_SHA`, use Node 22, and use `persist-credentials: false`.

The proof workflow must not patch source/test files and must never be merged into `phase6-sync-integration-h`.

---

# 11. REQUIRED VERIFICATION

Run the following in one bounded proof workflow.

## A. Exact candidate / invariant preflight

Verify:

- `git rev-parse HEAD == H_U5_P4_CANDIDATE_SHA`;
- `src/contracts/**` tree = `0db68ced179825f929008b502335210260ca2ce3`;
- canonical `dev/evidence/_ca-output.md` blob = `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
- contract-freeze whole-file blob = `b675e0fc9776d03892a4309231b91a4bf0a84b93`;
- contract-freeze first-16,296-byte prefix hash = `fe527c76137b2cd578ef7050ee3444498b21a5e0`;
- tracked working tree clean.

Print Git, Node, and npm versions.

## B. Dependency and static gates

Run directly:

`npm ci`

`npm run typecheck`

`npx tsc -p tsconfig.test.json`

`git diff --check H_U5_P4_ENTRY_HEAD...H_U5_P4_CANDIDATE_SHA`

All must return exit code `0`.

## C. Focused H-U5-P4 verification

Run exactly:

`node --test .test-build/test/phase5-group-d-active-run-integration.test.js`

Capture the real process exit code.

Required acceptance:

- tests: `3`;
- pass: `3`;
- fail: `0`;
- cancelled: `0`;
- skipped: `0`;
- todo: `0`;
- exit code: `0`.

All three original scenario names and behavioral assertions must remain present.

Specifically prove:

- scenario 15 performs exactly the intended single remote trash and is accepted;
- scenario 24 still refuses the stale mutation, leaves update count at zero, surfaces attention, and does not self-trigger a Changes read;
- scenario 25 reaches the hardened remote update, preserves the active-run pause/release semantics, consumes the deferred remote change in a later serialized reconciliation, converges `b.md` locally, preserves `a.md` remotely, and commits the expected later cursor.

## D. V1.3 / H critical-regression gate

Run:

`node --test .test-build/test/phase6-foundation-failure-provenance.test.js .test-build/test/phase6-h-sync-integration.test.js`

Capture the real process exit code and totals.

Required unchanged surface:

- tests: `82`;
- pass: `69`;
- fail: `13`;
- cancelled: `0`;
- skipped: `0`;
- todo: `0`;
- exit code: `1` solely because the 13 already-classified G-owned adversarial-model failures remain.

Acceptance:

- all 17 V1.3 foundation tests PASS, including C15/C16;
- all H-I1 through H-I8 markers PASS;
- only the same 13 G-owned failures remain;
- no new non-G failure appears.

Do not repair G.
Do not weaken or remove G discovery.

## E. Whole-repository measurement

Run:

`npm test`

directly, with no `tee` or pipeline that can mask the real process exit code.

Capture immediately:

- real numeric npm exit code;
- tests;
- pass;
- fail;
- cancelled;
- skipped;
- todo.

Authoritative semantic acceptance:

- scenario 15 is no longer failed;
- scenario 25 is no longer cancelled;
- scenario 24 remains passing;
- no new failure/cancellation is introduced by P4;
- the 13 G-owned failures remain untouched;
- all remaining obsolete-fixture families remain outside this unit.

Expected bookkeeping if no unrelated branch drift occurs:

Current baseline:

`687 total / 631 pass / 31 fail / 25 cancelled`

P4 removes exactly one failure and one cancellation by converting both to passes, so expected post-P4 totals are:

- tests: `687`;
- pass: `633`;
- fail: `30`;
- cancelled: `24`;
- skipped: `0`;
- todo: `0`;
- exit code remains `1` because known classified failures still remain.

If aggregate totals differ materially from that exact two-result improvement, do not broaden the repair. Record the discrepancy and stop for supervisor review.

## F. Build / package gate

Run:

`npm run build`

Require exit code `0` and capture the existing build-verifier outputs.

## G. Final invariant recheck

After execution, verify again:

- HEAD is still `H_U5_P4_CANDIDATE_SHA`;
- tracked source/test working tree is clean;
- `src/contracts/**` tree remains `0db68ced179825f929008b502335210260ca2ce3`;
- canonical evidence blob remains `d4c610ccbc6cbbd6d58f548525239bb6d61e1f73`;
- contract-freeze whole-file blob and immutable prefix remain exact;
- candidate scope remains exactly the one authorized test file.

Upload focused, critical, full-suite, and build logs as a single proof artifact and record artifact ID/digest.

---

# 12. EVIDENCE CLOSURE

Only after the candidate passes the required P4 gates, create/update exactly:

`dev/evidence/_ca-output-agt-h-ca-stage-2a-phase-6-sync-hardening-serial-integration-06.md`

Record at minimum:

- agent identity;
- `H_U5_P4_ENTRY_HEAD`;
- `H_U5_P4_CANDIDATE_SHA`;
- exact changed-file manifest;
- concise fixture modernization description;
- focused proof run/job IDs and exact 3/3 totals;
- H/V1.3 critical-gate exact totals and confirmation that only 13 G failures remain;
- whole-repository exact totals and real numeric exit code;
- static/type/build outcomes;
- frozen-authority checks;
- proof artifact ID/digest;
- PR #45 state;
- disposable proof branch/workflow head;
- remaining unauthorized H-U5 families.

Then commit/push that evidence-only update and record:

`H_U5_P4_EVIDENCE_SHA`

Required comparison:

`H_U5_P4_CANDIDATE_SHA...H_U5_P4_EVIDENCE_SHA`

must contain exactly the H-06 evidence file and no source/test/planning change.

Do not modify:

- canonical `dev/evidence/_ca-output.md`;
- H-01/H-02/H-03/H-04/H-05 evidence;
- any prior task file.

---

# 13. REMAINING WORK — EXPLICITLY NOT AUTHORIZED HERE

P4 does not authorize repair of any other H-U4 family, including:

## Remaining OLF-PHYSICAL after P4

- `test/phase5-group-d-acceptance.test.ts`;
- `test/phase5-group-d-conflict-destruction-integration.test.ts`;
- `test/phase5-group-d-recovery-coordination-integration.test.ts`;
- `test/phase5-group-d-surface-lifecycle-integration.test.ts`;
- `test/phase6-alpha-plan-errors-stability.test.ts`.

## OLF-FAKE-AUTH

The fake-auth legacy family remains entirely outside P4.

## G worker defects

All 13 G-owned adversarial-model failures remain outside H repair authority.

## Production-structure normalization

The supervisor has separately recorded a mandatory production-structure normalization pass before final closure. It addresses development-phase/workstream naming that leaked into production architecture, including `phase6-sync-integration.ts` and related phase-named runtime surfaces.

That pass is **not P4 authority**. Do not rename, move, split, or normalize production modules in this task.

## H-FINAL / H-U6

Do not start final closure.

Do not run a physical iPhone synchronization.

---

# 14. STOP CONDITIONS

If fixture-only modernization exposes a genuine production semantic defect requiring any `src/**` change, stop with exactly:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

If execution capability is unavailable or a required invariant cannot be established, stop with exactly:

`BLOCKED — SUPERVISOR DECISION REQUIRED`

If turn capacity is exhausted before completion, preserve resumable evidence and stop with exactly:

`TURN CAPACITY EXHAUSTED / CONTINUATION REQUIRED — H-U5-P4 NOT COMPLETE`

On successful completion, stop with exactly:

`H-U5-P4 COMPLETE — READY FOR SUPERVISOR REVIEW — DO NOT START H-U5-P5 OR H-FINAL`
