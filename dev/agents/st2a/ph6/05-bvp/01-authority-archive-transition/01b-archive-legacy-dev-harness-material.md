# BVP-S01B — Archive Legacy Development-Harness Material

## 0. Status

**Agent name:** `agt-brain-bvp-s01-authority-archive-01`  
**Prompt maturity:** COMPLETE / NON-EXECUTABLE  
**Primary work package:** BVP-S01 — Authority / Archive Transition

This file is a historical contract record. It authorizes no new work and MUST NOT be re-executed.

## 1. Objective

Move superseded legacy-harness planning, tasking, evidence, state, and mixed active-authority material beneath the inert legacy archive while preserving historical traceability and retaining valid non-harness authority in clean active replacements.

## 2. Required End State

The completed archive transition established that:

- superseded legacy harness `dev/**` artifacts were moved beneath `dev/archive/legacy-validation-harness/**`;
- active tasking/planning/evidence no longer treated the legacy harness as current;
- mixed authority/handoff files were preserved historically and replaced by clean active forms where required;
- the archive retained historical review value;
- an archive manifest accounted for moved/replaced material;
- active BVP authority remained outside the archive.

## 3. Required Semantics and Invariants

- Archive operations preserved history rather than erasing it for search cleanliness.
- Valid non-harness authority survived transition.
- `dev/archive/**` became non-authoritative for ordinary implementation grounding.
- Every active legacy-harness match required classification; no ambiguous current authority could remain.
- This documentation/evidence transition could not redesign production synchronization or begin replacement-platform implementation.

## 4. Dependencies

This child depended on accepted S01A authority supersession and the authoritative archive-transition plan.

## 5. Engineering Discretion

The worker could choose ordinary move/manifest mechanics where not fixed by repository state, but could not decide that ambiguous mixed authority was safe to leave active, silently delete historical material, or reinterpret archived harness designs as replacement architecture.

## 6. Material Edge / Failure Cases

The child would have failed if:

- legacy tasking remained discoverable as active authority;
- differently named/content-matched legacy evidence escaped classification;
- a mixed file lost valid non-harness authority;
- active files linked into the archive as current executable/task authority;
- archive history was deleted instead of preserved.

## 7. Acceptance Criteria

Historical completion required a complete archive transition, clean active authority, classified legacy matches with no unexplained remainder, preserved historical evidence, and no product-source implementation changes.

## 8. Non-Goals

This child did not:

- remove `src/validation/**` or production runtime coupling;
- delete ordinary product tests/diagnostics;
- implement `test-platform/**`;
- create the simulator, runner, live-device agent, or scenario catalog.

## 9. Historical Completion

S01 was completed before the current PHX-CI acceptance model was adopted. Its accepted historical evidence remains authoritative for this completed child.

**Established result:** legacy development artifacts archived; active authority clean; archive manifest complete.

## 10. Stop

No work is authorized by this file.
