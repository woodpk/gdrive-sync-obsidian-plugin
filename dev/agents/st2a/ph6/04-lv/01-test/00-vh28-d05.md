# VH28 — D05 Offline Editing and Reconnect

Agent: `agt-ca-p6-vh28-d05-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh28-d05-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh28-d05-scenario-01.md`

## Assignment
Implement only the D05 hybrid harness scenario and focused tests. The scenario must establish independent harness-owned mobile and Windows fixtures, pause at a named resumable human checkpoint for genuine mobile offline state, permit local mobile editing while offline, drive an independent Windows change/sync while mobile remains offline, pause for reconnect, then resume normal mobile synchronization and verify both changes survive and converge without inappropriate global authority, duplicate, or conflict.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No value is deferred for supervisor insertion.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `D05-offline-reconnect.md`, VH15 evidence, and current checkpoint/coordinator/scenario APIs. Use the approved human-checkpoint controller for real offline/reconnect; do not pretend an injected transport failure proves physical device-offline behavior. iPhone or iPad may fill the mobile role. Do not alter production network semantics, frozen H0 contracts, scenario text, or `src/contracts/**`. Do not perform the physical checkpoint during this build.

Required end state: D05 is resumable across both human checkpoints, records acknowledgements/device identity in evidence, and has deterministic tests for pause/resume, wrong/stale checkpoint rejection, convergence, and genuine-conflict-only behavior.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or D05 PASS claim.