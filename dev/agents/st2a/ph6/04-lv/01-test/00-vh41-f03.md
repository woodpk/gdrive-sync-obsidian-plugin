# VH41 — F03 Disable / Uninstall / Device Unlink Safety

Agent: `agt-ca-p6-vh41-f03-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh41-f03-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh41-f03-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh41-f03.ps1`
## Assignment
Implement only the F03 hybrid harness scenario and focused tests. The scenario must capture pre-state/evidence and then pause/resume through named human checkpoints for: plugin disable/re-enable, uninstall/reinstall, and device deauthorization/unlink/reconnect through the normal supported flow. Around each external action, verify local vault/shared remote content remain intact, the other valid participant remains usable, remote identity is not silently replaced, and re-entry requires the appropriate production validation/reconciliation.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field requires later insertion.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `F03-lifecycle-disable-unlink.md`, VH15 evidence, current plugin-data/device-unlink/lifecycle code, and checkpoint/coordinator APIs. The harness must not attempt to uninstall itself programmatically or simulate uninstall with an internal exception. Persist only non-secret checkpoint/coordination data necessary for post-reinstall resume. Back up relevant local settings/state through the approved harness mechanism; never expose tokens. iPhone or iPad may fill the mobile role. Do not alter frozen H0 contracts or `src/contracts/**`.

Required end state: the full F03 sequence is represented as durable resumable checkpoints with objective pre/post assertions; focused tests prove checkpoint ordering, stale-run rejection, secret exclusion, and preservation assertions using lifecycle fakes without claiming real uninstall behavior passed.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh41-f03.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical F03 PASS claim.