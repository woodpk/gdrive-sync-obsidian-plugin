# VH21 — C08 Windows Rename/Move → Mobile Move

> **Current verification policy override (applies only if this historical task is re-executed):** GitHub Actions and the retired BRAIN-owned `dev/scripts/run-phx-ci.ps1` are prohibited. Automated verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. A task-specific repository-controlled launcher under `dev/scripts/` may orchestrate the run but must not reimplement PHX-CI core sequencing; it must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, run task-focused/change-set verification and complete repository verification through PHX-CI (including required tests, build, repository checks, artifact checks, and `git diff --check`), and leave canonical evidence in `dev/_ca-output.md` and `dev/_ca-output.json`. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS`. Any GitHub-hosted CI, workflow/run/job, synthetic-merge, or retired-runner language below is historical context only and is not executable current policy.


Agent: `agt-ca-p6-vh21-c08-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh21-c08-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh21-c08-scenario-01.md`

## Assignment
Implement only the C08 harness scenario and focused tests. Establish the required trusted harness-owned fixture lineage, rename/move it on Windows, drive Windows then mobile production synchronization, assert identity-preserving remote/mobile move, stable Drive ID, old-path absence, unchanged bytes/hash, no delete+create replacement, and no unrelated mutation.

## Executable base gate
At execution, run `git fetch origin --prune`; resolve `BASE_SHA` as the exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless `dev/evidence/_ca-output-agt-ca-p6-vh15-validation-mode-runtime-canary-01.md` exists there and begins `STATUS: COMPLETE`. Create the required branch from exactly that SHA. No prompt field requires pre-dispatch editing.

## Authority / boundaries
Read the complete harness plan, DEC-301–DEC-310, shared live-validation protocol, `C08-windows-move-ios-move.md`, VH15 evidence, and the current scenario extension APIs. Encode prerequisite fixture state inside this scenario rather than requiring C07 to have been physically run during build. Do not modify scenario acceptance text, frozen H0 harness contracts, `src/contracts/**`, or shared harness helpers unless the task cannot be completed through their established extension points; if blocked by a contract deficiency, stop with `CONTRACT CHANGE REQUEST`. Do not run live Drive/mobile validation.

Required end state: C08 maps one-to-one to its existing package, uses production move semantics, proves stable identity and old-path absence, and has deterministic success plus delete/create-substitution and unexpected-plan hard-stop tests.

## Verification / evidence
Run focused tests, `npm run check`, and `git diff --check`. Fix only session-introduced defects. Commit implementation/tests first; then write the evidence file beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording resolved base SHA, implementation SHA, changed files, commands/results, deviations, and blockers; commit evidence separately.

Stop without merging, promoting, releasing, running physical validation, or claiming C08 PASS.