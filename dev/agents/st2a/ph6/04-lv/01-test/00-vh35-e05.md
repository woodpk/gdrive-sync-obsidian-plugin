# VH35 — E05 Destructive Circuit Breaker

Agent: `agt-ca-p6-vh35-e05-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh35-e05-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh35-e05-scenario-01.md`

## Assignment
Implement only the E05 harness scenario and focused tests. Populate a dedicated disposable validation vault/remote with enough deterministic harness-owned fixtures to exceed the current production destructive-safety threshold, create a legitimate-looking bulk local deletion, request the production preview only, and verify destructive execution is blocked before mutation, affected scope/recovery checkpoint is surfaced for explicit review, and no force bypass exists. This scenario must never approve or execute the bulk destructive plan.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No pre-dispatch substitution is required.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `E05-destructive-circuit-breaker.md`, VH15 evidence, current destructive-safety/planning code, sandbox, fixture manager, and plan-assertion APIs. Do not alter threshold policy merely to make the test easy; determine the current production threshold from repository code/config and generate bounded disposable fixture scale accordingly. Do not modify frozen H0 contracts or `src/contracts/**`; do not execute any destructive plan.

Required end state: E05 deterministically reaches the real production circuit breaker in automated tests, proves no mutation occurs after the block, captures scope/checkpoint evidence, and rejects any bypass path.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, threshold observed, bounded fixture count, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E05 PASS claim.