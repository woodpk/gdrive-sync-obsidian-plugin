# VH27 — D04 Delete vs Independent Modify

Agent: `agt-ca-p6-vh27-d04-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh27-d04-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh27-d04-scenario-01.md`

## Assignment
Implement only the D04 harness scenario and focused tests. From a common trusted harness-owned text BASE, execute delete-vs-independent-modify in both device directions. Between subcases re-establish a clean trusted BASE through normal harness/production paths. In both directions assert that the modification survives, deletion conflict is surfaced, no newest-wins/destructive guess occurs, and modified content remains recoverable/live as required.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field requires later filling.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `D04-delete-vs-modify.md`, VH15 evidence, and current deletion/conflict/harness APIs. Do not fabricate authoritative state to reset between subcases; use approved fixture and production seams. Do not modify production semantics, existing scenario text, frozen H0 contracts, or `src/contracts/**`. Do not run physical validation.

Required end state: D04 contains both role-reversed subcases, independently proves modification preservation and surfaced conflict, and has deterministic tests rejecting silent deletion or timestamp/newest-wins behavior.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED` with base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release/live validation or physical PASS claim.