# VH16 — C03 Mobile Edit → Windows Download/Update

Agent: `agt-ca-p6-vh16-c03-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh16-c03-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh16-c03-scenario-01.md`

## Assignment
Implement only the C03 harness scenario and focused tests. Reproduce the authoritative C03 sequence with a harness-owned fixture: mobile edit, mobile production sync, Windows production sync, Windows plan requiring the expected `download-update`, then verify bytes/hash, remote identity, state commit after verified replacement, and no unrelated change.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`. Create the required branch from exactly that SHA. No prompt data is filled in before dispatch.

## Authority / boundaries
Read the complete harness plan, DEC-301–DEC-310, shared live protocol, `C03-ios-update-windows-download.md`, VH15 evidence, and current harness scenario APIs. Preserve C03's existing behavior; do not change scenario text, frozen harness contracts, or `src/contracts/**`. Do not run live Drive/mobile validation.

Required end state: C03 maps one-to-one to the existing package, delegates through harness/production seams, hard-stops on unexpected plans, and has deterministic fake/integration tests for success and a representative fail-closed plan mismatch.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first, then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files and results. Stop without merge/promotion or C03 PASS claim.