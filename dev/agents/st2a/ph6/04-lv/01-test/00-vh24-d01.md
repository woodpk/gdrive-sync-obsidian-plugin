# VH24 — D01 Concurrent Non-Overlapping Text Merge

Agent: `agt-ca-p6-vh24-d01-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh24-d01-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh24-d01-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh24-d01.ps1`
## Assignment
Implement only the D01 harness scenario and focused tests. Establish one harness-owned text fixture as a common trusted BASE on Windows and mobile; before either observes the other's change, apply deterministic non-overlapping edits on different lines; drive production synchronization in the required sequence; verify a clean three-way merge with no conflict copy, both edits exactly once, cross-device/remote convergence, and no timestamp-based authority.

## Executable base gate
At execution, `git fetch origin --prune`; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. Nothing in this prompt needs pre-dispatch substitution.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `D01-clean-text-merge.md`, VH15 evidence, and current scenario/harness APIs. Do not alter production merge policy, scenario acceptance text, frozen H0 contracts, `src/contracts/**`, or execute live validation. Build prerequisite BASE state through existing harness/production seams, not direct authoritative-state fabrication.

Required end state: D01 maps one-to-one, proves clean-merge orchestration and exact-once combined content, and has deterministic tests covering success plus unexpected conflict/newest-wins-style outcome rejection.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh24-d01.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED` with resolved base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release/live validation or physical PASS claim.