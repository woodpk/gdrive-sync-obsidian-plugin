# VH31 — E01 Interrupted Mutation / Restart Recovery

Agent: `agt-ca-p6-vh31-e01-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh31-e01-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh31-e01-scenario-01.md`

## Assignment
Implement only the E01 hybrid harness scenario and focused tests. Using a harness-owned sufficiently large fixture, orchestrate production synchronization to a deterministic observable in-flight mutation checkpoint after work has begun, persist the scenario checkpoint/evidence, and pause for the operator to terminate Obsidian. On later restart/resume, the scenario must verify durable completed work is recognized, uncertain work is reconciled from physical reality, no false-success/duplicate/data loss occurs, and recovery/replanning ends converged.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field requires later insertion.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol including OBS-01, `E01-interruption-restart.md`, VH15 evidence, current durable-intent/effect/recovery code, and checkpoint APIs. Implement the pause/resume machinery but do not substitute an internal exception for genuine process termination and do not perform the real kill/restart in this build session. Never directly rewrite authoritative state. Do not modify frozen H0 contracts or `src/contracts/**`.

Required end state: E01 can deterministically reach/persist the external termination checkpoint and resume afterward; focused tests simulate lifecycle restoration only to prove orchestration, durable checkpoint validation, stale-run rejection, and post-restart recovery assertions—not to claim the physical restart test passed.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or E01 physical PASS claim.