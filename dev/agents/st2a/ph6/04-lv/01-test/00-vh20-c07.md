# VH20 — C07 Windows Edit → Mobile Download/Update

Agent: `agt-ca-p6-vh20-c07-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh20-c07-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh20-c07-scenario-01.md`

## Assignment
Implement only the C07 harness scenario and focused tests. From a trusted harness-owned fixture, edit on Windows, drive Windows then mobile production sync, assert the expected mobile `download-update`, verified replacement, content/hash equality with remote, and no unrelated mutation.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt data is filled before dispatch.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `C07-windows-update-ios-download.md`, VH15 evidence, and current harness APIs. Do not change scenario text, frozen harness contracts, `src/contracts/**`, or run live validation.

Required end state: C07 maps one-to-one, uses production update/replacement paths, verifies exact content and no unrelated change, and has deterministic success plus stale/unexpected-plan hard-stop tests.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files/results. Stop without merge/promotion or physical PASS claim.