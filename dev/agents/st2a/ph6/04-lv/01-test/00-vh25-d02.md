# VH25 — D02 Concurrent Overlapping Text Conflict

Agent: `agt-ca-p6-vh25-d02-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh25-d02-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh25-d02-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh25-d02.ps1`
## Assignment
Implement only the D02 harness scenario and focused tests. From a common trusted harness-owned text BASE, make deterministic incompatible edits to the same region on Windows and mobile before either sees the other; drive production sync; assert an unresolved text conflict with no newest-wins overwrite, preservation of both complete versions and provenance, exercise one existing supported explicit resolution through the production conflict path, and verify safe final convergence.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from that SHA. No prompt field is deferred.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `D02-true-text-conflict.md`, VH15 evidence, and current conflict/harness APIs. Do not invent a harness-only conflict resolver, modify production conflict semantics, scenario text, frozen H0 contracts, or `src/contracts/**`. Do not run physical validation.

Required end state: D02 maps one-to-one, verifies both originals remain recoverable until resolution, uses a real supported resolution action, and includes deterministic tests rejecting silent overwrite, incomplete preservation, or premature authority commit.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh25-d02.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording resolved base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release/live validation or physical PASS claim.