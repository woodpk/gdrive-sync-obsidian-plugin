# VH32 — E02 Ambiguous Network Outcome After Remote Mutation

Agent: `agt-ca-p6-vh32-e02-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh32-e02-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh32-e02-scenario-01.md`

## Assignment
Implement only the E02 harness scenario and focused tests. Use a harness-owned disposable fixture and the approved deterministic fault layer to withhold/lose the client result only after a real production remote mutation may have been dispatched. Verify the operation remains uncertain until remote reality is re-observed; recovery/retry creates no duplicate and overwrites no valid content; authoritative state commits only after physical verification.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No supervisor substitution is required.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol/OBS-01, `E02-ambiguous-network-outcome.md`, VH15 evidence, current Drive mutation/recovery code, and H4 fault APIs. The injector must not directly edit Drive, manufacture success/not-applied certainty, or bypass production durable-effect logic. Do not alter frozen H0 contracts, `src/contracts/**`, or run live Google Drive validation in this build.

Required end state: E02 maps one-to-one, fault evidence identifies the response-loss point, and deterministic tests prove uncertainty is retained until observation and that duplicate/overwrite/early-commit outcomes fail.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E02 PASS claim.