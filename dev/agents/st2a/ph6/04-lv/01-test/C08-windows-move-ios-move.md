# C08 — Windows Rename/Move → iOS Move

Follow the shared protocol. Requires C07 PASS.

Rename `test-win-c06.md` to `test-win-c08-renamed.md` on Windows; sync Windows, then iPhone.

PASS: same remote Drive ID is preserved; iOS applies the corresponding move; old path disappears; no delete+create replacement or unrelated mutation occurs.

Stop before C09.
