# VH37 — E07 Safe Cancellation

Agent: `agt-ca-p6-vh37-e07-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh37-e07-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh37-e07-scenario-01.md`

Local verification launcher: `dev/scripts/verify-vh37-e07.ps1`
## Assignment
Implement only the E07 harness scenario and focused tests. Create several deterministic disposable operations or one bounded transfer large enough to expose an active production run, begin reviewed execution through the production path, invoke the existing supported cancellation action at a deterministic observable active point, then reconcile. Verify no new operation starts after cancellation acceptance, current atomic work is durably completed or safely classified, authoritative state reflects only verified effects, and subsequent reconciliation converges without duplicate or loss.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt field requires later substitution.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `E07-safe-cancellation.md`, VH15 evidence, current cancellation/executor/recovery code, and H4 cancellation fault/checkpoint APIs. Do not invent a harness-only cancellation path or terminate work by throwing arbitrary test exceptions. Do not alter production cancellation semantics, frozen H0 contracts, or `src/contracts/**`. Do not execute live physical validation.

Required end state: E07 uses the real cancellation boundary, deterministically observes acceptance, proves no later operation starts, verifies durable classification and eventual convergence, and has tests for cancellation before/at active work without false-success state.

## Verification / evidence
GitHub Actions are prohibited. Authoritative verification must run locally through the centralized PHX-CI revision pinned by `phx-ci.json.framework.sha`. Create/use the thin repository-controlled launcher `dev/scripts/verify-vh37-e07.ps1`; it is orchestration only and must not duplicate PHX-CI core sequencing. The launcher must validate the PHX-CI checkout HEAD against the exact pin, preserve the user's active/control checkout without reset/clean/switch/stash, capture complete output and exit codes, and invoke PHX-CI for separate task-focused/change-set verification and complete repository verification, including the required focused tests, typecheck, full repository tests, build, repository checks, artifact checks, and `git diff --check`. Canonical PHX-CI evidence is `dev/_ca-output.md` and `dev/_ca-output.json`, with historical runs under `dev/test-results/`; this task's `dev/evidence/` file remains separate. `STATUS: COMPLETE` is forbidden unless PHX-CI reports `PASS / PASS / PASS` and the task-specific focused gates pass.
Within the PHX-CI focused/change-set dimension, run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical E07 PASS claim.