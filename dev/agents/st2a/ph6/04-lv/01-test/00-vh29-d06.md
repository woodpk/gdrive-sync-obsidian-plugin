# VH29 — D06 Stale Device Return Safety

Agent: `agt-ca-p6-vh29-d06-scenario-01`  
Repository: `woodpk/gdrive-sync-obsidian-plugin`  
Required branch: `phase6-vh29-d06-scenario`  
Evidence: `dev/evidence/_ca-output-agt-ca-p6-vh29-d06-scenario-01.md`

## Assignment
Implement only the D06 harness scenario and focused tests. Orchestrate a harness-owned fixture with an actually stale/offline secondary device condition without forging production authority. While that participant is absent, establish newer authoritative changes including one attested deletion; on return, verify reconciliation occurs before stale state can authorize destructive propagation or resurrect deleted content. If the configured stale condition cannot be safely reached or induced under the production rules, the scenario must return exactly `BLOCKED — STALE CONDITION NOT SAFELY INDUCIBLE` rather than altering authority.

## Executable base gate
At execution, fetch origin; resolve `BASE_SHA` as exact head of `origin/phase6-vh15-validation-mode-runtime-canary`; hard-stop unless its evidence begins `STATUS: COMPLETE`; create the required branch from exactly that SHA. No prompt substitution is required.

## Authority / boundaries
Read the harness plan, DEC-301–DEC-310, shared protocol, `D06-stale-device.md`, VH15 evidence, current stale-device/tombstone/reconciliation code, and harness APIs. Do not add a test hook that directly rewrites device age, authority generations, tombstones, or trusted state merely to manufacture staleness. Do not change scenario text, frozen H0 contracts, or `src/contracts/**`. Do not run physical stale-device validation.

Required end state: D06 supports safe orchestration/checkpointing, exact defined BLOCKED classification when necessary, and tests proving stale destructive proposals/resurrection hard-stop while valid reconciliation can proceed.

## Verification / evidence
Run focused tests, `npm run check`, `git diff --check`. Commit implementation/tests first; then evidence beginning exactly `STATUS: COMPLETE` or `STATUS: BLOCKED`, recording base SHA, implementation SHA, changed files, commands/results, deviations, blockers; commit evidence separately.

Stop without merge/promotion/release or physical PASS claim.