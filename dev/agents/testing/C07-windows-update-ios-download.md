# C07 — Windows Edit → iOS Download/Update

Follow the shared protocol. Requires C06 PASS.

Edit `test-win-c06.md` only on Windows; sync Windows, then iPhone.

Expected iOS plan: one `download-update`.

PASS: iOS content equals Windows/remote content, verified replacement commits cleanly, and no unrelated path changes.

Stop before C08.
