# C06 — Windows Create → iOS Download/Create

Follow the shared protocol.

Create `test-win-c06.md` on Windows, sync Windows, then iPhone.

Expected iOS plan: one `download-create`.

PASS: one remote object exists and both devices contain identical bytes/hash with converged authority and no conflict/duplicate.

Stop before C07.
