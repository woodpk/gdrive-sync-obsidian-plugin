# C03 — iOS Edit → Windows Download/Update

Follow the shared protocol. Requires C02 PASS.

Edit `test-ios-c02.md` only on iPhone, sync iPhone, then sync Windows.

Expected Windows plan: one `download-update`, no upload or conflict.

PASS: Windows bytes equal the iOS/remote version, state commits after verified replacement, remote identity remains coherent, and no unrelated path changes.

Stop before C04.
