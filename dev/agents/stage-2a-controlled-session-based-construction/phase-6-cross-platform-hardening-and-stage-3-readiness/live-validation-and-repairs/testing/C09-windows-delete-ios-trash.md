# C09 — Windows Delete → iOS Recoverable Delete

Follow the shared protocol. Requires C08 PASS.

Delete the C08 fixture on Windows; sync Windows, then iPhone.

PASS: exact remote object is trashed; iOS removes the live file through recoverable deletion semantics; tombstone/state converge; no unrelated changes.

Stop.
