# B03 — Windows Local Rename/Move → Remote Move

Follow the shared protocol. Requires B02 PASS.

Rename/move the B02 fixture to `test-file-02-b03-renamed.md` without changing content.

Preview must show one identity-preserving move/rename, not delete+create or upload replacement. Execute once.

PASS: the same Drive object ID now represents the new path; old path has no live object; content is unchanged; BASE/mapping converge to the new path; no unrelated mutation.

Stop before B04.
