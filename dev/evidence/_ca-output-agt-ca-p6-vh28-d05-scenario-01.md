STATUS: BLOCKED

# VH28 — D05 Offline Editing and Reconnect Evidence

- Agent: `agt-ca-p6-vh28-d05-scenario-01`
- Repository: `woodpk/gdrive-sync-obsidian-plugin`
- Required branch: `phase6-vh28-d05-scenario`
- D_SERIES_COMMON_BASE_SHA = `c6daa20ad287f395a99cf88943465a9ecc3159dd`
- Preserved pre-H6B branch: `phase6-vh28-d05-scenario-pre-h6b-restart`
- Preserved pre-H6B HEAD: `38df16aa5bb5cb28f068d448d4cdeacdabc9deee`
- Implementation HEAD: `7be8f763d5b0a458351890144a777dcb6c49ea74`

## CONTRACT CHANGE REQUEST

D05 is blocked on a shared H6B production-path capability.

The fixed validation production-path driver reports accepted production execution requests with `productionOutcomeEstablished: false`, and the current validation-safe execution result does not expose the exact production diagnostic run identity associated with an asserted production cycle.

D05 performs six production preview/assert/execute cycles. Within task-owned scope it cannot prove, using exact run-correlated production diagnostics, that each accepted execution reached its required terminal production result.

Required shared capability:

1. bind each asserted validation production cycle to the exact production diagnostic run identity generated by the real production execution path;
2. expose that identity through a validation-safe shared seam after the production request is accepted;
3. allow the shared verifier/scenario layer to require terminal outcome evidence for that exact diagnostic run, including `terminal-product-result` / `final-reconciliation-stable` proof as applicable;
4. preserve `productionOutcomeEstablished: false` until terminal production evidence is actually observed;
5. use stable run identity/correlation rather than timestamps, latest-event selection, or task-local success flags;
6. preserve the real production controller, executor, synchronization authority, and network semantics.

D05 does not attempt to implement this shared capability.

## Task-local corrections after rejection

### Checkpoint evidence failure semantics

`D05ConnectivityCheckpointController.consumeResume()` now:

- snapshots candidate acknowledgement/device evidence only from an exact run/checkpoint/device resumable state;
- delegates to the frozen VH13 `HumanCheckpointResumeController.consumeResume()`;
- records checkpoint evidence only when VH13 returns exact `status: "resumed"` with the expected checkpoint and resume step;
- records the exact validation run identity with each checkpoint observation;
- exposes `records(run)` filtered by exact run identity.

D05 scenario logic now uses only `records(request.run)`, so observations retained from a prior D05 run cannot satisfy the current run's offline/reconnect gates or final evidence.

### Final retained-conflict guard

D05 final verification now reads the current production surface through a task-owned read-only callback and fails if the final surface retains any conflict whose path is either D05 fixture path.

This supplements, and does not weaken, the existing plan assertion and exact remote path-level duplicate proof.

The scenario remains blocked overall until the shared terminal execution-provenance seam exists.

## Deterministic regression coverage added

Focused tests now prove:

- resume-adoption failure leaves D05 paused;
- failed resume adoption records no checkpoint evidence;
- failed resume adoption does not perform mobile/Windows edits, additional production execution, or final evidence;
- the same checkpoint adapter can retain observations from multiple runs without allowing an old run's observations to satisfy a new run;
- `records(run)` returns only records for the exact run identity;
- a clean reconnect/reconcile plan combined with a retained D05-path production conflict surface produces D05 FAIL and no final evidence;
- exact remote content, two-device BASE authority, live mappings/no tombstones, and no-outstanding-effect verification remain present.

## Verifier hardening

`dev/scripts/verify-vh28-d05.ps1` now hard-gates:

- exact frozen common base `c6daa20ad287f395a99cf88943465a9ecc3159dd`;
- preservation branch `phase6-vh28-d05-scenario-pre-h6b-restart` exactly at `38df16aa5bb5cb28f068d448d4cdeacdabc9deee`;
- exact implementation delta `BaseSha..ImplementationHead` limited to:
  - `src/validation/scenarios/d05-offline-reconnect.ts`
  - `test/validation-d05-offline-reconnect.test.ts`
  - `dev/scripts/verify-vh28-d05.ps1`
- absence of dedicated task evidence at `ImplementationHead`;
- pre-verification branch tail limited to the dedicated task evidence file;
- rejection of frozen/shared production-driver/runtime/contract surfaces, product/network/sync prefixes, peer D-series scenario paths, or any unexpected implementation path;
- available VH24/VH25/VH26/VH27/VH29 peer evidence, failing if any available evidence records a different `D_SERIES_COMMON_BASE_SHA`;
- committed-range `git diff --check`;
- installed immutable PHX-CI runtime identity from `phx-ci.json`;
- post-verification frozen-base, preservation-branch, and peer-common-base rechecks.

Current remote peer evidence observed during this repair:

- VH24: `c6daa20ad287f395a99cf88943465a9ecc3159dd`
- VH25: `c6daa20ad287f395a99cf88943465a9ecc3159dd`
- VH26: `c6daa20ad287f395a99cf88943465a9ecc3159dd`
- VH27: `c6daa20ad287f395a99cf88943465a9ecc3159dd`
- VH29: evidence not currently available

Non-PASS PHX-CI reporting now surfaces:

- PHX-CI verdict;
- Change-set verification verdict;
- Repository verification verdict;
- Overall verification verdict;
- Task exit code;
- evidence commit;
- local evidence branch;
- evidence publication status;
- publication issue;
- runtime process exit code.

GitHub Actions are not used.

## Implementation scope at implementation HEAD

Exact changed files from the frozen base to `7be8f763d5b0a458351890144a777dcb6c49ea74`:

- `dev/scripts/verify-vh28-d05.ps1`
- `src/validation/scenarios/d05-offline-reconnect.ts`
- `test/validation-d05-offline-reconnect.test.ts`

No `src/contracts/**`, H6B production-path driver, H6B plan-assertion engine, validation-mode runtime, production synchronization/network semantics, peer D-series scenario, or `phase6-integration` file was modified.

## Static audit

PASS — implementation delta contains exactly the three allowed task-owned implementation files.

PASS — preservation branch still resolves exactly to `38df16aa5bb5cb28f068d448d4cdeacdabc9deee`.

PASS — `origin/phase6-integration` still resolves exactly to `c6daa20ad287f395a99cf88943465a9ecc3159dd`.

PASS — no trailing whitespace or merge markers detected in the three implementation files.

PASS — no unscoped `checkpoints.records()` calls remain; D05 checkpoint observations are queried by exact run identity.

PASS — verifier audit found no unsafe ordinary unbraced `$variable:` interpolation hazards.

## Verification status

Authoritative PHX-CI execution is intentionally NOT RUN.

The supervisor has rejected local PHX-CI execution until the shared terminal execution-provenance capability is repaired and promoted. No bootstrap is provided and no PASS / PASS / PASS claim is made.

Blocker remains the CONTRACT CHANGE REQUEST above.
